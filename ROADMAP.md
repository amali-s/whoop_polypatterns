# WHOOP Dashboard — Build Roadmap

A phased plan for building a web UI that connects to your WHOOP app, pulls your data, and visualizes it with custom charts plus a daily questionnaire.

**Stack:** React + TypeScript + Vite (frontend) · D3.js (visualizations) · **Vercel** (hosting + serverless functions for WHOOP OAuth) · **Supabase** (Postgres for tokens + data + questionnaire) — both on **free tier**

> **Accuracy note:** The WHOOP API details below were verified against the WHOOP developer docs in June 2026 and reflect **API v2** (v1 is being deprecated — see the v1→v2 migration guide). APIs change; verify every endpoint, scope, and URL against the live docs at https://developer.whoop.com before relying on it. Anything I'm not fully certain of is flagged inline.

---

## Key architectural decision (read first)

> **Data-source decision (confirmed June 2026):** The **WHOOP cloud API (OAuth)** is the **primary, confirmed source** — it fully supports **WHOOP 5.0** and delivers every metric automatically. A direct-from-band Bluetooth path (read raw sensor data off the strap, decode and score it locally, no cloud) was evaluated. It works today only for **WHOOP 4.0** (via projects like `openwhoop`); on the **5.0** the deep-metric protocol (recovery, strain, sleep) is **not yet publicly reverse-engineered**, so that path is captured as an exploratory R&D track in **Phase 7** — not on the critical path. _(Verify project/protocol status before relying on it; this space moves fast.)_

Your WHOOP "app" gives you a **Client ID** and **Client Secret** from the WHOOP Developer Dashboard. The **Client Secret must never live in frontend code** — a React/Vite bundle is fully visible to anyone. The locked-in architecture:

```
[ React + Vite SPA ]      [ Vercel Serverless Functions ]     [ WHOOP API ]
   charts + UI         →     /api/auth, /api/callback,      →    OAuth + data
   (hosted on Vercel)        /api/whoop/* — hold Client
                             Secret in Vercel env vars,
                             do OAuth token exchange
                                      │
                                      ▼
                            [ Supabase Postgres ]
                            encrypted tokens + WHOOP
                            data + questionnaire
```

**Why this combo:** Vercel hosts the Vite frontend _and_ the OAuth/serverless functions in one project, so the Client Secret stays server-side in Vercel's env vars. Supabase holds tokens and data. Both run comfortably on free tier for a single user.

> **Free-tier reality:** No BAA on free tier — fine here, since you store only your own WHOOP data and HIPAA doesn't bind individuals. The privacy lever is in _your_ code: encrypt tokens at the app level before storing (Phase 1.4) and never log secrets/health fields. Note Supabase **pauses inactive free projects** — verified against the live docs 2026-07-05: pause after **7 days of low database activity**, requests to a paused project return **HTTP 540 "Project Paused"**, and the project does **NOT auto-resume on request** — the owner must click "Resume project" in the Supabase dashboard (90-day restore window). Handled in Phase 2.5.

---

## Phase 0 — Foundations & project setup

**Goal:** A running repo, a scaffolded app, and a clear architecture before any feature work.

- [ ] **0.1 Create the GitHub repository**
  - Create repo (private recommended while it holds health data wiring).
  - Add `.gitignore` (Node template) — confirm `.env`, `node_modules`, build output are ignored.
  - Add `README.md` (project summary, setup steps) and this `ROADMAP.md`.
  - Choose a license (or keep private with none).
  - Set up branch protection on `main` (require PR) if you want discipline.
- [ ] **0.2 Scaffold the frontend**
  - `npm create vite@latest` → React + TypeScript template. _(Verify the exact command/flags against current Vite docs — the CLI prompts change.)_
  - Add ESLint + Prettier; pick a styling approach (see Phase 4).
  - Confirm dev server runs and commits a clean baseline.
- [ ] **0.3 Set up Vercel** — connect the GitHub repo to a Vercel project; confirm the Vite app deploys. Add an `/api` folder for serverless functions (Vercel auto-deploys these as endpoints). Add a health-check function to confirm it runs. Document required vars in `.env.example` (no real secrets committed); set the real ones in Vercel's env-var settings.
- [ ] **0.4 Set up Supabase** — create a free-tier project, **pick the region nearest you**, and save the project URL + keys into Vercel env vars (never the frontend). Create initial tables: `whoop_tokens`, plus placeholders for synced data and questionnaire (schema firmed up in Phase 2/5). Enable Row Level Security.
- [ ] **0.5 Repo structure** — single repo (monorepo): Vite app at root, serverless functions in `/api`. Simplest for a solo Vercel build.
- [ ] **0.6 Create `Skills.md`** — a living index of the skills, tools, and knowledge each phase relies on (e.g. `theme-factory`, `figma-to-code-generator`, Figma MCP, D3, OAuth 2.0). Pull from the "Skills / knowledge to lean on" notes in this roadmap so it's a single reference for what to use where.
- [ ] **0.7 Create `design.md`** — the design spec / source of truth for the UI: design tokens (colors, typography, spacing), layout/grid, component inventory, and the chart→WHOOP-metric mappings (from Phase 4). Feeds Phase 3 and Phase 4 and keeps styling decisions in one place.

**Skills / knowledge to lean on**

- **Knowledge:** Git/GitHub basics (branches, PRs), Vite project setup, monorepo layout, `.gitignore` hygiene, environment-variable management.
- **Tooling:** I can run the scaffolding and Git commands for you in the workspace, and I have a GitHub/web browser path if you want me to help create the repo interactively.

---

## Phase 1 — WHOOP API connection (auth)

**Goal:** Securely authenticate a WHOOP member and obtain a usable access token.

WHOOP uses **OAuth 2.0 Authorization Code flow**. Verified endpoints (June 2026):

- Authorize: `https://api.prod.whoop.com/oauth/oauth2/auth`
- Token: `https://api.prod.whoop.com/oauth/oauth2/token`
- API base: `https://api.prod.whoop.com`
- Scopes: `offline`, `read:profile`, `read:recovery`, `read:sleep`, `read:workout`, `read:cycles`, and (worth verifying) `read:body_measurement`. The `offline` scope is what returns a **refresh token**.

- [x] **1.1 Register redirect URI** in the WHOOP Developer Dashboard — registered prod URI: `https://whooppolypatterns.vercel.app/api/callback` (also set as `WHOOP_REDIRECT_URI` in Vercel). For local dev use `http://localhost:3000/api/callback`. Redirect URIs must match exactly — note the path is `/api/callback`, not `/callback`.
- [x] **1.2 Build the authorize redirect** — a Vercel function (`/api/auth`) constructs the authorize URL with `client_id`, `redirect_uri`, `response_type=code`, `scope`, and a `state` param (CSRF protection).
- [x] **1.3 Handle the callback** — `/api/callback` exchanges the `code` for `access_token` + `refresh_token` (Client Secret from Vercel env vars, server-side only). **Done:** implemented in `api/callback.ts` (state check, code exchange, encrypted token upsert, session cookie, SPA redirect with `whoop_error` banner on failure); live-tested end-to-end on prod and covered by `npm run test:callback` (see Phase 2.5 notes).
- [x] **1.4 Token storage with app-level encryption** — before writing tokens to the Supabase `whoop_tokens` table, **encrypt them in the function** (e.g. AES-256-GCM using a key held only in Vercel env vars). This way even the DB operator sees ciphertext, not your raw WHOOP tokens. Never put tokens in `localStorage` or in the frontend. _(Verify the exact crypto API you use against current Node docs.)_
- [x] **1.5 Token refresh** — implemented in `api/_lib/refresh.ts`: `ensureFreshTokens(userId)` / `getValidAccessToken(userId)` read the row via `getWhoopTokens`, and when the access token is within a 5-minute skew of expiry (or has unknown expiry) POST `grant_type=refresh_token` to the token endpoint, then re-encrypt + persist the rotated tokens and recompute `expires_at`. Wired into `/api/session` so SPA polling keeps the token fresh without ever exposing token material. **Confirmed against live docs (June 2026):** (a) refresh rotates — each refresh returns a NEW `refresh_token` and invalidates the old access token, so we persist the new refresh token every time (and keep the existing one if WHOOP ever omits it, since the column is NOT NULL); (b) the refresh body needs `grant_type`, `refresh_token`, `client_id`, `client_secret`, and `scope` (we resend `offline`, per the docs' sample payload), creds in the body like the code exchange; (c) TTL is variable, reported per-response via `expires_in` (sample shows `3600`), so we never hardcode it except as the same 1h fallback `callback.ts` uses. **Assumed/not separately verified:** exact refresh-token (not access-token) lifetime is not documented as a fixed value, so re-auth is still required if the refresh token itself ever lapses or is revoked. **Concurrency caveat (documented + mitigated, not locked):** two simultaneous serverless requests could both refresh and the second would be rejected because the first rotated the token — on a refresh-rejected response we re-read the row and use the freshly stored token rather than corrupting the row. Unit-tested via `npm run test:refresh` (no creds, mocks `fetch`): no-refresh-when-fresh, rotate+persist, omitted-refresh-token, rejected-refresh, rotation race, and missing row.
- [x] **1.6 Auth state in the UI** — "Connect WHOOP" button → redirect → connected state.

**Skills / knowledge to lean on**

- **Knowledge:** OAuth 2.0 Authorization Code flow, `state`/CSRF, refresh-token rotation, secure secret storage, CORS between SPA and proxy.
- **Reference:** WHOOP OAuth guide and the "Authenticating with Passport" tutorial (Passport.js strategy) if you use Node — verify it's current.

---

## Phase 2 — Data layer (fetch, model, store)

**Goal:** Reliable, typed access to WHOOP data feeding the charts.

WHOOP v2 resources you'll likely use: **Cycles** (physiological cycles / strain), **Recovery** (recovery %, HRV, RHR), **Sleep** (stages, performance), **Workouts**, **Profile**, **Body Measurement**.

- [x] **2.1 API client** — implemented in `api/_lib/whoop.ts` (server-only): one typed function per resource (`getProfile`, `getBodyMeasurement`, `getCycles`, `getRecovery`, `getSleep`, `getWorkouts`) over a central request core. Auth header injected via `getValidAccessToken()` (Phase 1.5) per request — no token material logged or leaked. Transparent cursor pagination (`fetchCollection`) plus a single-page helper (`fetchCollectionPage`), guarded by configurable max-pages/max-records. Retries 429/5xx/network with exponential backoff + jitter and honors `Retry-After` (seeds Phase 2.7); never retries other 4xx; 401 → distinct `WhoopAuthError`; all failures throw a typed `WhoopApiError` (status + endpoint + WHOOP error body). **Verified against the live OpenAPI spec** (base `…/developer`; paths `/v2/cycle`, `/v2/recovery`, `/v2/activity/sleep`, `/v2/activity/workout`, `/v2/user/profile/basic`, `/v2/user/measurement/body`; pagination `limit`≤25 + `nextToken` → `records[]`/`next_token`; `start`/`end` ISO date-time; scopes already granted by `/api/auth`). Records stay generic (`unknown[]`) for Phase 2.2 to type. Live smoke test: `npm run test:whoop` (`scripts/test-whoop.mjs`) — all six endpoints 2xx, cursor advances, typed error path, no tokens/health fields printed.
- [x] **2.2 TypeScript types** — implemented in `api/_lib/whoop-types.ts`: `WhoopProfile`, `WhoopBodyMeasurement`, `WhoopCycle`, `WhoopRecovery`, `WhoopSleep`, `WhoopWorkout`, each derived field-by-field from a real captured payload (`scripts/capture-whoop-samples.mjs` → gitignored `whoop-samples/`, deleted after review — never committed). Cycle/Recovery/Sleep/Workout are discriminated unions on `score_state` (`'SCORED'` → `score` populated; `'PENDING_SCORE' | 'UNSCORABLE'` → `score: null`). Wired into `api/_lib/whoop.ts` as the default generic (`getCycles<T = WhoopCycle>`, etc.), still overridable. Human-reviewed field-by-field against the raw JSON — full match, no mismatches. **Known gaps from a single-sample capture** (flagged inline as `TODO(verify)`, not blocking): only the `'SCORED'` arm of each union was observed live; `WhoopCycle.end`, `v1_id`, and `WhoopWorkoutScore.distance_meter`/`altitude_gain_meter`/`altitude_change_meter` were only seen `null` (unconfirmed whether always-nullable vs. just unset in this sample — e.g. `distance_meter` was null because the captured workout was pilates, no GPS). `npm run typecheck:api`, `npm run lint`, and `npm run test:whoop` (rerun locally against live Supabase — the build sandbox couldn't reach Supabase, so this had to be confirmed on your machine) all pass. **2.2 is fully closed.**
- [x] **2.3 Caching / sync strategy** — store fetched WHOOP data in Supabase so charts read from your DB, not WHOOP on every load. **Webhooks** are available (configurable v1/v2 in the dashboard) to get pushed updates; or a scheduled sync (Vercel Cron on free tier — verify current limits).
  - **Webhook `*.deleted` handling (2026-07-03, closes the delete gap from the 2026-07-02 review):** `*.deleted` events now HARD-delete the matching cached row (was: create/update only). Confirmed against the live webhook docs that deletions are distinct per-resource types (`recovery.deleted` / `sleep.deleted` / `workout.deleted`) carrying a v2 resource id. `deleteRecord()` (`api/_lib/sync.ts`) maps each id to the right column: workout/sleep UUID → `whoop_id`; **recovery → `raw->>'sleep_id'`, since the webhook id is the linked _sleep_ UUID whereas the stored `whoop_id` is the cycle_id** (this also resolves the old `TODO(verify)` on recovery id semantics in `api/webhook.ts`). Hard delete (no schema change) — the tables are a pure WHOOP cache. Signature verification / counts-only logging unchanged. Unit-tested via `npm run test:webhook` (`scripts/test-webhook.mjs`, no creds, mocks `fetch`): per-resource delete keying, bad-signature and stale-timestamp → 401 with the DB untouched, unconfigured-secret → 501, and uncached record → `deleted=0` no-op.
- [x] **2.4 Supabase schema** — tables for cycles, recovery, sleep, workouts keyed by date; this gives chart history and lets you join questionnaire data (Phase 5). **Implemented (2026-07-03):** `supabase/migrations/0003_typed_columns.sql` adds nullable typed columns to all four synced tables (idempotent `ADD COLUMN IF NOT EXISTS`, matching 0001/0002; 0001/0002 untouched since they may already be applied). Columns are read off `api/_lib/whoop-types.ts` (verified against the live 2026-06-30 capture, Phase 2.2) and cover the Phase 4 chart metrics: cycles → strain/kilojoule/HR/start/end/offset; recovery → recovery %, RHR, HRV, SpO2, skin temp, calibrating; sleep → stage totals, performance/efficiency/consistency %, respiratory rate, disturbances, nap, start/end/offset, plus `need_from_sleep_debt_milli` (added beyond the original list for chart 4.3's sleep-debt-area option — it lives only in `score.sleep_needed`); workouts → sport, strain, HR, kilojoule, distance, start/end/offset. `raw jsonb` stays the source of truth — typed columns are a read optimization only. Row builders in `api/_lib/sync.ts` populate them from the already-typed records; score-derived columns are written NULL whenever `score_state !== 'SCORED'` (WHOOP returns `score: null` in those states — never guessed). Day-derivation, (user_id, day) dedupe, and webhook-delete logic (2.3) unchanged. `npm run typecheck:api`, `npm run lint`, `npm run test:webhook` pass in the sandbox; **the migration has NOT been applied to live Supabase and sync has NOT been live-tested — both need to happen from your machine** (see PROJECT-STATE.md). Rows cached before the migration keep NULL typed columns until re-upserted — backfill by re-running `npm run sync:whoop` over the desired window.
- [x] **2.5 Handle free-tier pause** — **Implemented & LIVE-VERIFIED (2026-07-05).** **Verified against the live Supabase docs first:** free projects pause after **7 days** of low database activity; a paused project's API returns the documented **HTTP 540 "Project Paused"**; and — correcting the roadmap's original assumption — a paused project does **NOT auto-resume when a request arrives**: the owner must click "Resume project" in the dashboard (90-day restore window). **Server:** DB failures are now classified by gateway status (traced against the installed `@supabase/postgrest-js` 2.108.2 source, not guessed: non-2xx statuses pass through on the builder result; fetch-level failures are caught and surface as the `status: 0` sentinel). `api/_lib/supabase.ts` exports `isDbUnavailableStatus()` (0/502/503/504/540/544 — infra-only codes PostgREST never produces for query/auth errors) + `DatabaseUnavailableError`; `tokens.ts`/`refresh.ts` throw it on those statuses; `/api/session` maps it to **`503 { connected:false, waking:true }`** (+ `Retry-After`) instead of the old flat 500, leaking no token material, status codes, or dependency names. Genuine failures (PostgREST 500, corrupt ciphertext, rejected WHOOP refresh) still return the flat 500. **Frontend:** new `'waking'` ConnectionState in `src/App.tsx`, driven by `src/session-check.ts` — retries `waking:true` responses and timeouts/network errors with capped exponential backoff (2s/4s/8s/8s/8s ≈ 30s budget; 10s per-attempt timeout), shows a spinner + honest copy meanwhile, and on exhaustion degrades to the disconnected screen with a "resume your project from the Supabase dashboard" hint (since resume is manual, the retry loop's job is riding out transient blips and being honest — not un-pausing). **Keep-warm cron: deliberately NOT added.** The daily `/api/sync` cron already reads `whoop_tokens` before touching WHOOP, so every run is real DB activity well inside the 7-day window; Hobby crons are once-a-day minimum so a second cron can't ping more often; and the failure modes that would stop sync before the DB (missing `CRON_SECRET`/env vars) would break a dedicated ping identically. A second cron would be pure redundancy. Unit-tested via `npm run test:session` (real `/api/session` handler, mocked fetch: paused 540 → 503 waking; unreachable status-0 → 503 waking; refresh-path UPDATE 540; genuine 500/corrupt-ciphertext NOT classified as waking; no-leak assertions) and `npm run test:backoff` (real retry loop, injected fetch/sleep: exact schedule, cap, no-retry on genuine failures, cancellation). **Follow-up (same day):** a live paused-DB test surfaced that a _logged-out_ browser hits the paused project through the OAuth callback (not `/api/session`, which short-circuits to `connected:false` before any DB read), so `api/callback.ts` now classifies the token-upsert failure the same way and redirects to the SPA with a `database_unavailable` banner instead of a cryptic "Failed to store tokens" (covered by `npm run test:callback`). **LIVE-VERIFIED end-to-end** against a genuinely paused project on Vercel prod: connected-user retry/resume screen and logged-out-user OAuth banner both behave as designed; resuming from the Supabase dashboard reconnects. See PROJECT-STATE.md.
- [x] **2.6 Data transforms** — `api/_lib/transforms.ts`: pure, import-free functions that turn the typed cache rows into chart-ready series — `buildDailySeries` (one point per calendar day across a range, gaps included, null-not-0), `buildSleepStageBreakdown` (per-night stacked stages in minutes, naps skipped) and a generic `buildRollingBaseline` (trailing-window mean with a `minSamples` floor, accessor-driven so it serves both HRV-over-baseline and RHR-over-sleep-debt). No side effects, no imports from sync/whoop/supabase. Unit-tested with hand-built fixtures via `npm run test:transforms`. Not yet exercised against real DB rows (fixtures only); the API endpoint that serves these to the frontend is Phase 4 wiring, not built here. See PROJECT-STATE.md.
- [x] **2.7 Rate-limit handling** — **Implemented (2026-07-05).** **Verified against the live docs first** (https://developer.whoop.com/docs/developing/rate-limiting, checked 2026-07-05): two limits per client — **100 requests/minute AND 10,000 requests/day**; every response carries draft-polli-ratelimit-headers-05 headers (`X-RateLimit-Limit` e.g. `"100, 100;window=60, 10000;window=86400"` — the FIRST value is the quota of whichever window the client is closest to exhausting, and the `;window=60`/`;window=86400` entries distinguish minute from day — plus `X-RateLimit-Remaining` and `X-RateLimit-Reset` in seconds); breach → HTTP 429. **Built in `api/_lib/whoop.ts`** (internal to `whoopRequest()` — no public signature changed): (a) pure `parseRateLimitHeaders()` reads the three headers off EVERY response (success and failure) and identifies the closest window (`minute`/`day`/`unknown`); (b) typed `WhoopRateLimitError extends WhoopApiError` carrying `{ window, remaining, resetSeconds }`, thrown for every 429 that is not retried further; (c) **day-window 429s now fail FAST with zero retries** — the old behavior burned the retry budget on 30s-capped backoffs that cannot refill a quota that resets in up to 24h; minute/unknown 429s keep the existing retry-with-backoff (a ≤60s window CAN clear inside the budget), with `Retry-After` still authoritative over the computed backoff; (d) **proactive throttle**: the last observed Remaining/Reset lives in module-level state (safe — the module is server-only and sync.ts issues every call sequentially), and when Remaining ≤ `RATE_LIMIT_SAFETY_BUFFER` (3) the next request sleeps out the reported reset instead of eating a real 429 — a zero-latency no-op at normal single-user volume, and deliberately skipped for day-window observations. `sync.ts`'s `classifyFetchError` now names the exhausted window in the resource error string so the cron log says WHY (`WHOOP rate limit hit (429, day window) @ …`) — counts/status/window only, no token material, per the existing logging discipline. **Unit-tested** via `npm run test:ratelimit` (`scripts/test-ratelimit.mjs`, no creds, mocks `fetch` + records `setTimeout`): header parsing incl. garbled/ambiguous → `unknown`; throttle no-op at comfortable Remaining and exact wait at/below the buffer; day-window near-limit NOT slept on; minute 429 retried with `Retry-After` winning exactly; day 429 → 1 fetch, 0 sleeps, typed error; exhausted budget → typed error; 5xx/network/plain-4xx paths unchanged. All pre-existing suites (`test:refresh`/`test:webhook`/`test:session`/`test:backoff`/`test:transforms`/`test:callback`) re-run and pass. **Live-verified (2026-07-05, this machine has `.env.local`):** `npm run test:whoop` passes end-to-end through the new code, and a one-off header capture on a real call confirmed the exact documented `X-RateLimit-Limit` format byte-for-byte (parsed as `minute`, Remaining 99, Reset 60). **Vercel plan/timeout confirmed via the Vercel API (not guessed): Hobby plan, Fluid Compute on, no `maxDuration` override → the current 300s default applies.** _Still unobserved live (flagged in the module header): a real 429 and the day-window-first header variant (first value `10000`) — the fail-fast path rests on the docs' "first value = closest limit" rule. Residual worst case: a persistent **5xx** storm can still sleep up to ~90s per resource (3 retries × 30s cap) ≈ 6 min across 4 sequential resources, which could exceed the 300s default — acceptable for a daily cron (the next run catches up), noted in PROJECT-STATE.md._

**Skills / knowledge to lean on**

- **Knowledge:** REST pagination, webhook handling + signature verification, TypeScript typing of API responses, data-shaping for time-series, caching patterns.

---

## Phase 3 — UI style & design system

**Goal:** A cohesive, good-looking dashboard shell before/while charts go in.

- [x] **3.1 Design tokens** — color palette (consider a WHOOP-like dark theme), typography scale, spacing, radii. Define as CSS variables / theme object. **Done (2026-07-06):** Neo Frutiger Aero tokens in `src/index.css`, documented in design.md §1 (contrast-hardened in 3.4).
- [x] **3.2 Layout shell** — header, sidebar/nav, responsive dashboard grid for the chart cards. **Done (2026-07-07):** sticky glass header + centered main column + bento grid from the confirmed Figma layout (design.md §2); no sidebar by design until Phase 5. See PROJECT-STATE.md.
- [x] **3.3 Component library** — reusable Card, ChartContainer, Loading/Empty/Error states, buttons, form controls. **Done (2026-07-08):** `src/components/` (Card, Button, ChartContainer, states, form primitives), documented in design.md §3. See PROJECT-STATE.md.
- [x] **3.4 Responsive + accessibility** — mobile breakpoints, color-contrast, keyboard nav, chart `aria`/text alternatives. **Done (2026-07-08):** verified 375/768/1024/1280px (header-wrap fix for a real 375px overflow), computed-ratio WCAG AA audit with four token darkenings + `--color-warning-text` (flagged in design.md §1; LOCKED chart palette untouched), 44×44px tap targets via hit-area extension, keyboard/focus audit (native elements, DOM-order focus, ≥4.6:1 focus outline), `prefers-reduced-motion` support, placeholder-tile aria audit, and the Phase 4 chart accessibility contract written into design.md §5.2. Known exception: the legacy OAuth banner's dismiss ✕ tap target (banner out of scope per §3).
- [ ] **3.5 Dark/light mode** (optional).

**Skills / knowledge to lean on**

- **Skill — `theme-factory`:** generate a cohesive color/font theme for the dashboard and apply it consistently.
- **Skill — `figma-to-code-generator`:** if you mock the dashboard (in Figma or as a sketch), turn it into React/TS components with design tokens and CSS animations.
- **Skill — `brand-guidelines`:** only if you want an Anthropic-styled look; otherwise build a WHOOP-flavored theme.
- **Tooling — Figma MCP:** I can generate or read Figma designs to drive the layout if you want to design first.
- **Knowledge:** design tokens, responsive CSS grid/flex, accessibility for data viz.

---

## Phase 4 — Data visualizations (D3.js)

**Goal:** Your six charts, built as reusable D3-in-React components, **plus the four bento-tile visualizations already implemented as static placeholders in the confirmed Figma layout** (design.md §2/§3: recovery donut, strain donut, period meter, skin-temp sparkline, calories/sleep stat cards) — these need real chart components and data wiring, not just the six chart types below. Each is a sub-project.

General D3+React pattern: let **React own the DOM / SVG container and state**, let **D3 own scales, axes, shapes, and transitions**. Build one solid reusable scaffold (responsive SVG, axes, tooltip, legend) and reuse it.

- [x] **4.0 Charting foundation** — responsive `<svg>` wrapper hook, shared scales/axes helpers, tooltip + legend components, animation/transition utility. **Done (2026-07-09, commit 7281946):** `src/components/charts/` — see PROJECT-STATE.md. (Checkbox ticked retroactively with 4.1; the work itself predates it.)
- [x] **4.1 Stacked bar chart** — suggested mapping: **sleep stages per night** (Awake / Light / Deep / REM stacked to total sleep), or strain contributors per day. **Done (2026-07-09):** generic `StackedBarChart` (`src/components/charts/StackedBarChart.tsx`, reusable for e.g. strain contributors later) rendering sleep stages below the bento grid, fed by the new `GET /api/sleep-stages?days=<n>` endpoint (first real caller of the 2.6 transforms against DB rows) via `src/hooks/useSleepStages.ts`; ChartContainer status driven from fetch state (this tile's 4.8 wiring). Built to the §5.2 contract (data-describing `<title>`/`<desc>`, sr-only data table from the same series, hover/focus tooltip parity + Escape, muted segment outlines, reduced-motion-gated entrance, bordered-swatch legend). Null nights render as visible gaps, never zero bars. Stage color mapping is a PROPOSAL in design.md §4, pending confirmation. NOT live-verified against real Supabase rows from this sandbox — see PROJECT-STATE.md.
- [x] **4.2 Combo chart #1 (line + area)** — suggested: **Recovery % (line) over Day Strain (area)** to see readiness vs. load. **Done (shipped earlier; checkbox ticked 2026-07-21 — it had been left unchecked despite the component being fully built and wired):** `RecoveryStrainComboChart` (`src/components/charts/RecoveryStrainComboChart.tsx`, barrel-exported) — d3 `line()`+`area()` with TWO independent y-scales (recovery fixed `[0,100]` left axis so 50% always sits mid-chart; strain `[0,max]` niced right axis so it isn't flattened) sharing one band x-scale; null days break both paths via `.defined()`, gaps occupying real axis width. §5.2: data-describing `<title>`/`<desc>`, sr-only `ChartDataTable` from the same prop, hover/focus tooltip parity + Escape on focusable recovery points, muted casing under the chart-6 recovery line (fails 3:1) with the chart-5 strain edge left bare (passes), reduced-motion-gated fade. Wired via `RecoveryStrainTile` (`src/App.tsx`) as a full-width row below the bento grid, fed App's shared 30-day `useDailySeries` (the fetch 4.11 later lifted into `App`); status maps 401/no-metric-in-window → empty, loading/error pass through. Live-verification status tracked with the other Phase 4 tiles (the standing residual; see 4.9's prod note).
- [x] **4.3 Combo chart #2 (line + area)** — suggested: **HRV (line) over a rolling baseline band (area)**, or RHR line over sleep-debt area. **Done (2026-07-21):** `HrvBaselineComboChart` (`src/components/charts/HrvBaselineComboChart.tsx`, new, barrel-exported with its props type) — d3 `line()`+`area()` plotting actual daily HRV (`DailyMetricPoint.hrvRmssdMilli`) over its own trailing rolling baseline. **Key departure from 4.2: ONE shared, zero-based y-scale** (not dual) — line and area are the SAME quantity (HRV in ms), so a second axis would make "above/below baseline" meaningless, and the zero floor makes the area an honest magnitude fill (4.2's strain-area precedent). **Baseline computed CLIENT-SIDE** from the same `data` prop via the pure, already-tested `buildRollingBaseline` (`api/_lib/transforms`, safe in the browser bundle per its own header) — no new endpoint, no second fetch. **Decisions:** `windowDays = 7` (a short "recent normal", deliberately named `HRV_BASELINE_WINDOW_DAYS` in-component, NOT App's 30-day `BASELINE_WINDOW_DAYS`); `minSamples` = `buildRollingBaseline`'s own default of 3 (a smoothed line, not a headline number — no stricter floor, unlike 4.12's 10); the first days of the 30-day window legitimately show no baseline (below `minSamples`) — an expected gap, not a bug. **Colors (§1/§4):** HRV actual = `--color-chart-7` (dark magenta, 4.5:1 — passes, so the line carries NO casing), baseline area = `--color-chart-4` (pale mustard, 1.60:1 — FAILS 3:1, so its area edge wears the muted hairline, mirroring 4.2's chart-6 recovery line). **§5.2:** data-describing `<title>`/`<desc>` (date span, HRV ms range, "N of M days show a baseline", missing-HRV count), sr-only `ChartDataTable` (day / HRV ms / recent baseline ms) from the same merged rows, hover/focus tooltip parity + Escape on focusable HRV points, reduced-motion-gated fade. **Mapping decision (supersedes design.md §4's locked chart-3 "population ideal-band"):** that population band is DEFERRED pending Phase 5 cycle-day data; the rolling-baseline variant shipped instead and is labelled **"Recent baseline"**, never "Ideal" (there is no population study behind it — see the design.md §4 note). **Wiring (`src/App.tsx`):** `HrvBaselineTile` replaces the `.combo-chart-placeholder` in the `bento-hrv` tile (keeps `bodyHeight={128}`; the chart pins its SVG to 128px, Sparkline-style; the legend's second entry is relabelled "Ideal HRV" → "Recent baseline" while keeping the chart-4 swatch), fed App's shared 30-day `useDailySeries` with status mapping identical to `RecoveryStrainTile` (401 or ready-with-no-HRV-in-window → empty, loading/error pass through). Gates `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run test:transforms`, `npm run format:check` all pass. **Open residual — NOT live-verified against real `/api/daily-series` data** (the standing Phase 4 residual); verified in the dev preview via the temporary vite `/api/daily-series` mock, then reverted.
- [ ] **4.4 Dot matrix #1** — suggested: **recovery calendar** (one dot per day, color = recovery zone red/yellow/green).
- [ ] **4.5 Dot matrix #2** — suggested: **sleep performance** calendar/matrix (dot size or color = % of sleep need met).
- [ ] **4.6 Dot matrix #3** — suggested: **strain** matrix, or a questionnaire-vs-recovery correlation matrix once Phase 6 exists.
- [ ] **4.7 Interactivity** — tooltips, hover, date-range filter, legend toggles shared across charts.
- [ ] **4.8 Loading/empty/error states** wired to the data layer.

> **Confirmed 2026-07-09** — mappings above accepted as-is (4.6 uses the strain-matrix variant, not the questionnaire-correlation variant, since Phase 5 doesn't exist yet). See design.md §4 for the locked table.

**Bento-tile visualizations added 2026-07-14** — these tiles already exist as static placeholder markup in the bento grid (Phase 3.2/3.3, design.md §2/§3) but were missing from this checklist. Same rigor as 4.1–4.8: real component, real data, §5.2 accessibility contract, `status` wired to fetch state (folds into 4.8).

- [x] **4.9 Recovery & Strain circular progress rings** — **Implemented (2026-07-14).** Reusable `ProgressRing` (`src/components/charts/ProgressRing.tsx`, barrel-exported): plain SVG `<circle>` arc via `stroke-dasharray`/`stroke-dashoffset` (`offset = circumference × (1 − fraction)`, fraction clamped, non-finite → 0) — no `d3-shape`, per this task's note. §5.2-compliant: `role="img"` + `<title>`/`<desc>` via `aria-labelledby` (ChartSvg pattern; the desc carries value + zone + scored day — rule 2's fallback for a single scalar), centered value is real SVG text in `--color-text` (never zone-colored, §5.1), entrance fill gated on `prefers-reduced-motion` in JS (`chartTransitionDuration`, StackedBarChart pattern) AND CSS (`charts.css` kills the transition). `noData` renders bare track + muted "—" + "no data yet" caption with an honest desc (mirrors the old placeholder's label). **Zone cutoffs VERIFIED against https://developer.whoop.com/docs/whoop-101/ (fetched 2026-07-14): green 67–100%, yellow 34–66%, red 0–33%** — constants live in `RECOVERY_ZONES` (`src/App.tsx`) with the citation; same doc confirms strain's 0–21 Borg scale (`STRAIN_SCALE_MAX`). Zone hues = fill-safe UI tokens (`--color-positive`/`--color-warning`/`--color-negative`); strain ring = `--color-chart-5` per §4. Wiring: ONE `useDailySeries(7)` call in `App` feeds both `RecoveryRingTile`/`StrainRingTile` (per-tile hooks would double-fetch identical rows); each tile scans the series from the end for its own latest non-null day, so a PENDING_SCORE/UNSCORABLE today falls through to yesterday (or to `noData`) via the standing null discipline. Fetch state → ChartContainer: loading/error pass through, 401 → `empty` with a connect-WHOOP message, ready-but-unscored → the ring's `noData` (NOT `empty`). Dead `.stat-donut*` CSS/markup removed. Lint + `tsc -b` + `typecheck:api` + prettier pass; visually verified in the dev preview via the temporary vite `/api/daily-series` mock (green 72%, red 28%, strain 6.3/21, strain-noData; `aria-labelledby` resolution confirmed in-browser), then the mock was fully reverted. **LIVE-VERIFIED on Vercel prod (2026-07-18, user-confirmed):** both rings render real values from a synced account, closing the "not yet seen against real `/api/daily-series` data" residual carried since implementation. **The live check was blocked for four days by an INFRASTRUCTURE fault, not a chart bug:** `CRON_SECRET` had never been set on the Vercel project, so `api/sync.ts` `isAuthorized()` fail-closed 401'd every nightly cron since deploy and Supabase held zero rows — the rings correctly showed `noData` (request succeeded, data genuinely absent) rather than `empty` (401) or `error` (non-OK), which is precisely how the state machine is supposed to behave and is what localized the fault. Fixed by generating the secret, adding it to Vercel **Production**, redeploying, and seeding via `npm run sync:whoop -- --days 30`. Note `CRON_SECRET` is still missing from `vercel-env-setup.md` (which enumerates only 7 vars) and `.env.example` — it is referenced in 2.5 below purely as a hypothetical, never as a setup step. Remaining 4.9 residual: the yellow recovery zone (34–66) is exercised only through the shared `recoveryZone()` path, not screenshotted.
- [x] **4.10 Period meter — dot-matrix cycle-day progress bar** — **Implemented (2026-07-18); NOT live-verified, and cannot be yet — no data source exists** (open residual below; do not read this checkmark as "the meter works against real data"). **Data source confirmed (2026-07-14): self-reported, not WHOOP API.** Verified directly against the live WHOOP v2 OpenAPI spec (`https://api.prod.whoop.com/developer/doc/openapi.json`) — the full resource list is Activity ID Mapping, Partner, User, Cycle, Recovery, Sleep, Workout; nothing exposes menstrual cycle data. **Entry point confirmed (2026-07-14): the Phase 5 daily journal's "Period" field** (`journal-stub-list` in `src/App.tsx`), not a standalone input — so the tile depends on Phase 5 before it can show real data.
  - **Three decisions CONFIRMED by the user (2026-07-18)** — these supersede the earlier "PROPOSAL, pending confirmation" items here and in design.md §4:
    1. **Episode-gap threshold = 3 days.** A `yes` day starts a NEW episode only when the gap since the previous `yes` day is **> 3 days** (strictly greater — a 3-day gap continues the episode); otherwise it continues the current one. Shipped as the exported constant `EPISODE_GAP_DAYS = 3` in `src/lib/cycle.ts`, with an in-code note that this is a chosen heuristic, not a clinically derived value.
    2. **Typical cycle length is ASKED, never assumed.** The app asks the user once, on their first logged period — that ask ships with Phase 5. It does NOT default to 28. Until a length exists (user-reported, or estimated from ≥2 logged episodes) the meter renders **text-only** ("Day 6" — no denominator, no dot row); an assumed 28-dot row is never rendered. Once ≥2 episodes exist, the estimate (mean start-to-start gap) is preferred over the user-reported value, and `cycleState`'s `lengthSource` reports which is in use so the UI labels the denominator truthfully.
    3. **Sequencing: component + logic + tests ship now;** the tile stays in its honest empty state until Phase 5 ships the journal's Period field. (The alternative — pulling the journal's one field forward — was declined.)
  - **What shipped:** `src/lib/cycle.ts` (new, pure, import-free — testable without a browser): tri-state `PeriodLog` (`'yes' | 'no' | null`, null = NOT LOGGED ≠ 'no'), `detectEpisodes` (explicit-`yes` days only; `'no'` and null are ignored IDENTICALLY for grouping so a missed logging day can never split one period in two; always recomputes from the FULL history so retroactive edits merge/split/shift boundaries correctly), `estimateCycleLength` (mean start-to-start gap, rounded; **null under 2 episodes** — callers must not substitute a default), `cycleState` (discriminated union `no-data` / `day-only` / `full`; `dayOfCycle = today − latest episode start + 1`, inclusive, keeps counting past the end of bleeding, and may exceed `cycleLength` — unclamped). Date arithmetic uses UTC-normalized integer day numbers, NOT local-midnight parsing or raw ms division — DST makes `(a − b) / 86400000` off by one (asserted in the tests). `src/components/charts/DotMatrix.tsx` (new, barrel-exported): plain SVG `<circle>` dots positioned by index — deliberately not d3 `scaleBand`, since a band scale over `0..n−1` reduces to `cx = i·slot + slot/2` (the ProgressRing no-machinery precedent). §5.2-compliant mirroring ProgressRing: `role="img"` + `<title>`/`<desc>` via `aria-labelledby` (`useId`), the desc carries the one scalar verbatim (= rule 2's fallback for a single value, no sr-only table), no hover surface (rule 3 n/a), the day number is real visible text (rule 4), entrance fade double-gated on reduced motion in JS (`chartTransitionDuration`) and CSS (`charts.css`). Filled dots = `--color-chart-3` (the confirmed shared skin-temp/period token, §1), track = `--color-border`. **Overflow** (`filled > total`, an overdue cycle) renders a fully filled row — never extra dots, never negative — with the label/desc carrying it legibly ("Day 31 of an estimated 28-day cycle"). `PeriodMeterTile` (`src/App.tsx`) replaces the static 28-span placeholder (dead `.period-bar`/`.period-seg*` CSS deleted, the 4.9 precedent); it calls `cycleState()` and renders all three kinds, so **the Phase 5 seam is just its `logs` / `typicalCycleLength` props**. At the time it resolved to `no-data`: a decorative all-track dot row, muted "—", and a caption naming the real reason ("no data yet — the Phase 5 journal isn't built"), honest `<desc>` to match, with ChartContainer left at `ready` since no fetch happened. **Both of those are superseded by 5.7**, which closed the seam: the tile now fetches its own history, drives ChartContainer's status from that fetch (the 4.9 rule unchanged — `empty` still means 401, and a dataless success is still the component's own `no-data` render), and the caption reads "no period logged in the last 100 days". A marked TODO at the render site requires surfacing limitation #6 (see design.md §4) in the UI once real data flows — silent inference must not ship.
  - **Tests:** `scripts/test-cycle.mjs` (`npm run test:cycle`), built exactly on the test-transforms pattern — real module import, synthetic hand-built fixtures, exact hand-computed expectations with the arithmetic in comments. 38 checks, **ALL PASS (2026-07-18)**, covering: zero-`yes` → no-data; first entry → day 1; consecutive/unsorted runs → one episode; a mid-period logging gap → one episode; **the exact >3-vs-≥3 boundary (a 3-day gap does NOT split)**; a 4-day gap → two; interleaved `'no'` days don't break episodes; null-heavy ≡ 'no'-heavy grouping; <2 episodes → null estimate → `day-only` with no 28 anywhere; round(28.5) = 29 estimation with `lengthSource: 'estimated'` beating a reported 30; retroactive merge AND split via full-history recompute; day 36 of an estimated 28 unclamped and positive; exact day counts across both DST transitions (spring-forward and fall-back).
  - **Open residual — the tile is NOT live-verified because no data source exists yet.** Only the `no-data` state has ever rendered in a browser (dev preview, 2026-07-18: 28-dot decorative track — all track-colored, claiming no cycle length — muted "—", honest caption/desc, `aria-labelledby` resolution confirmed in-page). The `day-only` and `full` renderings are unit-tested logic plus untested-in-anger markup; verify them against real journal data when Phase 5 ships, and only then close this residual.
  - **Schema requirement this creates for Phase 5.1 (flagging now, applies there):** the "Period" journal field must be **tri-state** — `yes` / `no` / `not logged` — not a plain boolean, because the algorithm only trusts explicit `yes` entries and must NOT treat a day the user simply didn't open the journal for as "no period" (that would incorrectly break episodes on ordinary missed-logging days). `src/lib/cycle.ts`'s `PeriodLog` type is the consuming contract. Phase 5.1 needs to decide the storage representation (e.g., nullable boolean, or an explicit enum) with this in mind — see the strengthened 5.1 entry.
- [x] **4.11 Skin-temp sparkline** — **Implemented (2026-07-18); NOT live-verified against real `/api/daily-series` data** (open residual below; do not read this checkmark as "seen working on prod"). **Decisions confirmed by the user (2026-07-18):** trailing window = **30 days** (matches `RECOVERY_STRAIN_DAYS`); units = **Celsius as stored, no °F conversion anywhere** (SVG, `<desc>`, and sr-only table all read the same stored value); null days **BREAK the line** via `.defined()`, never interpolated across.
  - **What shipped — data path:** `skin_temp_celsius` was already selected by `api/daily-series.ts` and typed on `RecoveryMetricRow`, but `buildDailySeries` never projected it; `DailyMetricPoint` now carries `skinTempCelsius` (with an in-code note that it's WHOOP 4.0+ hardware only, so long all-null runs are EXPECTED, not a bug — see 0003_typed_columns.sql), gated through the same `scored()` guard as the other recovery fields so a PENDING/UNSCORABLE row's stale value can never leak. `scripts/test-transforms.mjs` extended (fixture builder takes an optional skin temp; new assertions: SCORED row carries 33.9 through; a PENDING row carrying a stale 34.5 yields null; a SCORED row without a reading yields null; the all-null-day check now includes the field) — **ALL PASS (2026-07-18)**.
  - **What shipped — component:** `src/components/charts/Sparkline.tsx` (new, barrel-exported with its props type): chrome-free d3 `line()` — NO axes/gridlines/legend/tooltip, per the Figma sparkline treatment. Deliberately fixed to `skinTempCelsius` (the combo chart's fixed-metric precedent). X is **index-based** over the day array — buildDailySeries emits one point per calendar day with no missing days, so points are evenly spaced by construction and `scaleTime` reduces to `i/(n−1)` across the inner width (the DotMatrix/ProgressRing no-machinery precedent, noted in-code). Y = `safeExtent` of the readings plus a ~5% symmetric pad, **deliberately NOT zero-based** (skin temp varies by well under 1 °C; a zero floor would flatten the line) — noted in-code as shape-of-trend, not magnitude comparison. Stroke/endpoint dot = `--color-chart-3` (§1/§4's skin-temp token; ALSO the period-meter token — a co-occurrence design.md §4 explicitly accepts, left un-"fixed"). Small explicit margins (not `DEFAULT_MARGIN`'s axis gutters) keep the 2px round-capped stroke and the latest-reading dot unclipped. Entrance is a stroke-dash draw-on using `pathLength=1` normalization (no `getTotalLength`), double-gated on reduced motion in JS (`chartTransitionDuration` — duration 0 skips the dash setup entirely) and CSS (`charts.css` kill), the 4.9/4.10 pattern. §5.2: renders through `ChartSvg` with a data-describing `<desc>` (date span, min–max range, "N of 30 days have a reading" — derived from the same array the line draws; honestly reports "no days have a reading" when empty); a real `ChartDataTable` (day, skin temp °C) from the same prop — ~30 values is a series, so the ProgressRing/DotMatrix desc-only shortcut does NOT apply — with nulls reaching the table as null so IT renders "no data"; no hover surface (rule 3 n/a, stated in the header comment); the latest reading is real visible text ("33.4°C") in `--color-text`, never the chart hue. `noData` (zero readings) mirrors ProgressRing/DotMatrix: empty plot, muted "—", caption naming the likely reason.
  - **What shipped — wiring (`src/App.tsx`):** `SkinTempTile` replaces the `.sparkline-placeholder` div (dead CSS deleted from App.css — the 4.9/4.10 precedent; note the placeholder's mustard `rgba(217,208,89,…)` gradient was a stand-in, not the token — the real chart follows §4's chart-3). **The 30-day `useDailySeries` fetch was LIFTED out of `RecoveryStrainTile` into `App`** and passed to both it and `SkinTempTile` as a prop — a second identical 30-day fetch would violate the 4.9 no-duplicate-fetch rule; `RING_DAYS=7` stays a separate fetch because the rings genuinely need a different window. Status mapping: 401 → `empty` (connect-WHOOP message), error/loading pass through, and **ready-with-all-null temps deliberately does NOT map to `empty`** (diverging from RecoveryStrainTile, commented in-code): all-null is the NORMAL pre-4.0-hardware case and `empty` would read as a broken connection — it falls through to the Sparkline's own noData state, whose caption says "no readings in the last 30 days — skin temp needs WHOOP 4.0 or newer". The tile drops ChartContainer's `bodyHeight={64}` per that component's own "Phase 4 charts drop the prop" guidance; the Sparkline owns a fixed 64px plot with the value line below.
  - **Gates (2026-07-18):** `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run test:transforms` (ALL PASS), `npm run format:check` — all clean. **Dev-preview verified via the temporary vite `/api/daily-series` mock, then the mock was FULLY REVERTED** (`git checkout vite.config.ts`): with synthetic 30-day data the line broke into exactly 4 segments at the seeded null gaps (`.defined()` confirmed), stroke computed to `#FF4978` (chart-3), the endpoint dot and "33.4°C" text rendered, the `<desc>` read "…ranging 33.0 to 34.0 °C; 25 of 30 days have a reading", and the sr-only table had 30 rows with 5 "no data" cells; the all-null mock variant rendered the noData state (empty plot, muted "—", WHOOP-4.0 caption, honest desc, no line/dot).
  - **Open residual — NOT verified against real data.** To close: on prod (or `vercel dev` with a real session), confirm (1) the tile renders a real line from a synced WHOOP 4.0+ account — or, on pre-4.0 hardware, the noData state, NOT `empty`/`error`; (2) the visible latest reading matches the WHOOP app's skin temp for that day; (3) gap days (unscored/missing recoveries) break the line rather than bridging it; (4) the sr-only table and `<desc>` quote the same stored Celsius values as the SVG. The draw-on entrance under an OS reduced-motion setting is also only code-verified (the JS gate renders the final state immediately; the CSS kill is untested in a real reduced-motion session).
- [x] **4.12 Calories & Sleep stat cards with trailing-average delta** — **Implemented (2026-07-19); NOT live-verified against real `/api/daily-series` data** (open residual below; do not read this checkmark as "seen working on prod"). **Decisions encoded in-code (per this task):** (1) Sleep total = the **STAGE SUM** (light + deep + REM) = the existing `DailyMetricPoint.totalSleepMilli`, NOT `total_in_bed − total_awake` — it's already shipped, naps are already excluded, and it matches chart 4.1's stacked bar (one definition of "sleep" per dashboard beats two that disagree). (2) Minimum-sample floor = **10** non-null baseline days — stricter than `buildRollingBaseline`'s `DEFAULT_MIN_SAMPLES = 3` (that 3 was tuned for a smoothed line, not a headline number); below 10 the card shows the value with a "not enough history yet" caption and no delta. (3) Today is **EXCLUDED** from its own baseline ("today vs. my normal") — diverges from `buildRollingBaseline`'s trailing-INCLUSIVE window.
  - **What shipped — data path:** `buildDailySeries` read `CycleMetricRow.kilojoule` but never emitted it; `DailyMetricPoint` now carries `kilojoule` (raw kJ — the kJ→kcal conversion is a DISPLAY concern kept OUT of the pure transform), gated through the same `scored()` guard as `strain` so a PENDING/UNSCORABLE cycle's stale value can't leak. `api/daily-series.ts` already selected the column — no server change. `scripts/test-transforms.mjs` extended: a SCORED cycle surfaces its kJ (9000), a PENDING cycle's stale 8888 → null (not 0), a day with no cycle row → null — ALL PASS.
  - **What shipped — math (`src/lib/stats.ts`, new, pure, import-free):** `baselineDelta(series, accessor, {windowDays, minSamples, excludeToday})` → discriminated union `no-value` (today's metric null) / `no-baseline` (< `minSamples` prior non-null days) / `full` (`{value, mean, delta, percentDelta, sampleCount}`). "today" = the last ascending-series point; the baseline is the non-null PRIOR days in the trailing window, today excluded. Mirrors `cycle.ts`'s no-imports contract (generic over any `{day}` point, UTC-normalized day arithmetic) so it's testable without a browser; callers get honest null-ish states, never a substituted default (the `estimateCycleLength` discipline). `scripts/test-stats.mjs` + `npm run test:stats` (new): all-null → no-value, exactly `minSamples−1` → no-baseline, exactly `minSamples` → full, today-null-but-baseline-present → no-value, a zero delta, `windowDays` bounding the lookback, `percentDelta` null on a zero mean, and a proof that today's own value is genuinely absent from the mean (`excludeToday` true vs false) — 30 checks, ALL PASS.
  - **What shipped — component (`src/components/charts/StatDelta.tsx`, new, barrel-exported):** one reusable presentational card; both tiles inject only formatters + captions. §5.2 mapped deliberately (stated in the header): rules 1–3 n/a (no SVG, no hover — the value and delta are real visible DOM text, strictly better than a hidden fallback table); **rule 4 is the live one** — the delta is a real text SENTENCE ("312 cal above your recent average"), the ▲/▼ is `aria-hidden`, and it uses `--color-text`/`--color-muted`, NEVER `--color-positive`/`--color-negative` (more calories or less sleep isn't semantically good or bad, and the recovery rings already spend those tokens); rule 5 — no animation (a static render, nothing to gate). Direction is taken from the ROUNDED display magnitude so a sub-unit diff reads "In line with your recent average", never "0 min above". Dead `.stat-value`/`.stat-trend` CSS removed from App.css (the 4.9/4.10/4.11 precedent); `.stat-delta*` added to charts.css.
  - **What shipped — wiring + conversion (`src/App.tsx`):** `SleepStatTile`/`CaloriesStatTile` replace the static `—:—hrs`/`— cal` tiles, both fed App's SHARED 30-day `useDailySeries` via a prop — no second fetch (the 4.9 rule). `BASELINE_WINDOW_DAYS = RECOVERY_STRAIN_DAYS` (NOT bumped to 31 — that constant is load-bearing for 4.2's/4.11's "last 30 days" copy); excluding today leaves ≤29 baseline days, so the visible caption says "your recent average", never "exactly 30". Sleep formats `7:32 hrs` (round-half-up millis→minutes, then h:mm) + delta in minutes; calories a whole-kcal integer (`2,384 cal`) + delta in kcal. **Conversion factor `KJ_PER_KCAL = 4.184`** — the THERMOCHEMICAL calorie (NIST: "cal_th = 4.184 J exactly", https://www.nist.gov/pml/special-publication-811/nist-guide-si-footnotes, fetched 2026-07-19), which is the nutritional-"Calories" convention; the International-Table calorie is 4.1868 J (~0.1% larger). **WHOOP does NOT publish which calorie their displayed figure means** — checked developer.whoop.com (whoop-101, the workout-data endpoint) and WHOOP support on 2026-07-19; their docs describe a proprietary BMR + heart-rate model and give no kJ→kcal factor. We ship 4.184 because it matches the nutritional convention and the ~0.1% gap is far below WHOOP's own calorie-estimate error; the constant carries the caveat and is the single place to change if WHOOP ever states otherwise. Status mapping mirrors SkinTempTile: 401 → empty (connect-WHOOP), loading/error pass through, ready-but-no-value falls through to StatDelta's own no-value/no-baseline (empty means "no session," not "no rows"); `bodyHeight` dropped.
  - **Gates (2026-07-19):** `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run test:transforms`, `npm run test:stats` (ALL PASS), `npm run format:check` — all clean. **Dev-preview verified via the temporary vite `/api/daily-series` mock, then FULLY REVERTED** (restored to HEAD): a full-state 30-day mock rendered Sleep "7:32 hrs" + "▼ 28 min below your recent average" and Calories "2,451 cal" + "▲ 300 cal above your recent average" (both hand-computed exact); a 6-day variant (today's sleep null, 5 prior calorie days < the floor) rendered Sleep's no-value state ("—" + "no sleep recorded for today yet") and Calories' no-baseline state ("2,151 cal" + "not enough history yet for an average").
  - **Open residual — NOT verified against real data.** To close: on prod (or `vercel dev` with a real session), confirm (1) the visible kcal matches the WHOOP app's Calories for that day — the one check that validates 4.184 against WHOOP's actual displayed figure, which their docs don't state; (2) which night maps to "today" for the sleep card given sync.ts's start-day-vs-wake-day attribution `TODO(verify)` — under lay-down attribution "today's" sleep is tonight's (still null), so the card may sit in no-value until the night is scored, and this hasn't been seen live; (3) that a real ≥10-day history yields a sensible delta while a <10-day account shows the no-baseline caption.
- [x] **4.13 Sleep stat card — fall back to the latest scored night, with a visible "as of" disclosure** — **Implemented (2026-07-21, at your direction).** 4.12 shipped Sleep with a strict today-only check (`baselineDelta`'s own no-fallback contract, by design at the time); this reverses that specifically for Sleep. **Rationale:** WHOOP finalizes sleep and Recovery from the same overnight session at the same moment on waking, and both are already pulled in the identical once-daily sync pass (`api/sync.ts`, `0 15 * * *` UTC cron) — so there's no reason Sleep should go blank on a sync-lag day when Recovery doesn't. Calories was deliberately left on the strict check: it genuinely accrues live through the day, so a null "today" there is a real state, not a lag artifact.
  - **What shipped (`src/App.tsx`):** new `latestScoredSlice()` helper — mirrors the existing `latestScored()` backward-scan used by the Recovery/Strain rings (4.9), but returns the series SLICED to end on the found day (not just the scalar) so `baselineDelta` still computes its trailing baseline correctly against that day. `SleepStatTile` now calls this instead of reading `points` directly.
  - **What shipped — "as of" disclosure (`StatDelta.tsx`, `charts.css`):** new optional `asOfLabel` prop on `StatDelta`, rendered as a second muted `--text-xs` line (`.stat-delta-asof`) under the value, only when the sleep tile's fallback day differs from the shared series' true last day (`formatRingDay`-formatted, e.g. "As of July 20"). Silent (prop omitted) on the common on-time day — no visual change from 4.12 in that case. Calories is unaffected (never passes the prop).
  - **Known gap, unchanged:** the disclosure only compares against the single found day; it doesn't distinguish "one day behind" from "several days behind" with different wording — both just say "As of [date]". Revisit if multi-day gaps turn out to be common in practice.
  - **Gates (2026-07-21):** `npx tsc -b`, `npm run lint`, `npm run format:check`, `npm run test:stats` all pass (stats.ts itself untouched, only the App.tsx call site and the new StatDelta prop). Not yet seen against real prod data — carries the same standing 4.12 residual.
- [x] **4.14 Bento time-range toggle (1 month / 3 months)** — **Implemented (2026-07-28).** The dashboard window is now user-selectable between 30 and 90 days, replacing the hard-coded `RECOVERY_STRAIN_DAYS`/`BASELINE_WINDOW_DAYS` = 30. Five tiles are affected — RecoveryStrain, HrvBaseline, SkinTemp, Sleep, Calories. `RING_DAYS` = 7 (Recovery/Strain rings), the period meter and the journal are deliberately UNTOUCHED: they read a single latest-scored day, not a range, so the toggle has nothing to change about them.
  - **Answering the pre-work questions.** (1) **No row-count ceiling to add** — `/api/daily-series` already clamped `?days=` to `MAX_DAYS = 90` (`api/daily-series.ts:42`), which is exactly the ceiling this feature needs; the endpoint was NOT modified. (2) **No new WHOOP API pressure at all** — the toggle only widens a READ against Supabase rows already synced; it never calls WHOOP. The 2.7 rate limits (100/min, 10,000/day) are therefore not re-litigated here, and no backfill was run — populating 90 days of history is a separate, user-run step (needs your `.env.local`). (3) Window/floor and placement decisions below.
  - **What shipped — 4.14.1 data-fetch strategy (`src/App.tsx`):** `RECOVERY_STRAIN_DAYS` is gone, replaced by `type RangeDays = 30 | 90` plus `useState<RangeDays>(readStoredRangeDays)`. The persisted value is read via a LAZY state initializer, not an effect — an effect would flash the 30-day view and fire a second, immediately-superseded fetch for anyone on 90. `readStoredRangeDays` falls back to 30 on anything malformed (absent key, non-numeric, out-of-union, or a throwing `localStorage` under Safari private mode); `storeRangeDays` swallows write failures, so storage being unavailable costs persistence, never a render. Key: `whoop-dashboard:range-days`. The shared `useDailySeries(rangeDays)` call replaces `useDailySeries(RECOVERY_STRAIN_DAYS)`; `useDailySeries(RING_DAYS)` is untouched. That hook already keyed its effect on `days` and already documented the mid-session change case, so no hook change was needed — its `cancelled` cleanup flag is also what stops a slow 30-day response from landing on top of a newer 90-day one. All 7 UI strings that interpolated the constant (2 subtitles, 2 empty-state captions, 3 table/no-data captions) now read the live value.
  - **Decision — baseline window TRACKS the range, min-samples does NOT.** `baselineDelta`'s `windowDays` is now the selected `rangeDays`, so "your recent average" means "the average over whatever window is currently selected". `BASELINE_MIN_SAMPLES` stays FIXED at 10 (not scaled to e.g. 30 at the 90-day setting): it is a floor on statistical confidence, not a fraction of the window. Scaling it would blank the 3-month view for exactly the sparse-history users who benefit most from a longer lookback, and 10 real days is no less trustworthy a mean because the window around it got wider.
  - **What shipped — 4.14.2 UI control (`src/components/RangeToggle.tsx`, `components.css`):** a generic segmented control — `role="radiogroup"` over native `<button role="radio">` with `aria-checked`, ROVING TABINDEX (one Tab stop for the group, selected option tabbable, rest `-1`), arrow keys on both axes plus Home/End, and selection following focus per the WAI-ARIA radio pattern. Native buttons keep Space/Enter for free. §5.2's chart contract deliberately does NOT apply verbatim — this is a control, not a chart, with no data to expose as a table — but §5.1's shell rules do: focus ring is the standard `2px solid var(--color-accent)`, the selected segment is an accent FILL (not `--color-border` alone, which §5.1 forbids as a control's only boundary) and selection is redundantly conveyed by `aria-checked`, and the `::after` hit-area extension gives a 44px-tall tap target (verified computed 44px). Labels are calendar language ("1 month"/"3 months") while values are exact day counts — the tiles' own copy still says "last 90 days", so nothing claims a precision it doesn't have.
  - **Decision — placement is additive, the Figma grid is untouched.** The toggle sits in the 1200px main column ABOVE `.bento-grid`, in the same shell-content area as the OAuth banner (`.range-toggle-row`, mirroring the grid's own 640px cap + centering so it lines up with the tiles it governs, right-aligned within that box). It was deliberately NOT forced into `.bento-grid`: design.md §2's confirmed Figma frame is a 430px mobile mockup with no reserved slot, and the grid is capped at 640px — fitting one in would mean editing the locked `grid-template-areas`. Nothing in the bento grid changed to accommodate it, so the placement is fully reversible.
  - **Commit shape:** 4.14.1 and 4.14.2 landed in ONE commit, not two. A `useState` setter is inherently unused until a control calls it, so 4.14.1 in isolation could not pass `npm run lint`/`tsc` — splitting it would have meant shipping a lint suppression to fake a green gate. Flagged rather than worked around.
  - **No new unit tests, deliberately.** `lib/stats.ts` and `api/_lib/transforms.ts` are byte-untouched (confirmed via `git status`); the change is UI state plus passing a variable where a constant used to be. `baselineDelta`'s `windowDays` was ALREADY a parameter with existing coverage in `test:stats` — feeding it 90 instead of 30 exercises no new logic path. Per the task's own guidance, no new test scripts were added.
  - **Gates (2026-07-28):** `npx tsc -b`, `npm run lint`, `npm run typecheck:api`, `npm run format:check`, and `test:transforms`/`stats`/`cycle`/`webhook`/`session`/`backoff`/`ratelimit`/`callback`/`refresh` — ALL PASS. `test:whoop` NOT run: it needs `.env.local` credentials and hits the live WHOOP API.
  - **Dev-preview verified via the temporary vite `/api/daily-series` mock, then FULLY REVERTED** (`git checkout vite.config.ts`, confirmed clean). Verified: default 30 on a cleared key with NO write on mount (the read path doesn't persist a value nobody chose); click and ArrowLeft/Right both flip `aria-checked` + roving `tabIndex` and write `localStorage`; the network log shows the range fetch moving 30→90 while the rings' `days=7` fetch stays fixed; all 7 captions re-render to "last 90 days"; the data-table fallbacks go 90→270 rows (30→90 days × 3 tables); the Sleep/Calories deltas genuinely recompute against the wider baseline (▲23 min / ▲233 cal at 30 vs ▼10 min / ▼104 cal at 90); a reload with the key set to 90 restores the 3-month view; and the ring, period-meter and journal tiles are BYTE-IDENTICAL across a toggle (string-compared, not eyeballed). No console errors; no horizontal overflow at 375px or desktop.
  - **NOT live-verified — carries the standing 4.12/4.13 residual.** (1) Nothing here has been seen against real WHOOP data; the mock's day-0-anchored sine wave makes the HEADLINE value differ between windows too, which real data would NOT do (a given day's value is fixed in the DB — only the DELTA should change with the range). (2) **The 3-month view is only as good as the synced history**: if fewer than ~90 days have been backfilled, the 90-day setting shows a wider window of mostly-null days, not more data. Running that backfill is your step. (3) Whether a 90-day `/api/daily-series` read stays comfortably inside Supabase free-tier response limits has not been measured against a real, fully-populated table.

- [x] **4.15 RHR combo chart (line + rolling baseline)** — **Implemented (2026-07-28).** 4.3 shipped only the HRV variant of the "metric over its own rolling baseline" combo chart, leaving RHR as a static `.combo-chart-placeholder` in the `bento-rhr` tile with legend copy ("Ideal RHR") that was never accurate. This closes that gap: `RhrBaselineComboChart` (`src/components/charts/RhrBaselineComboChart.tsx`, new, barrel-exported with its props type) is the direct RHR sibling of `HrvBaselineComboChart` — same shape, same decisions, reused deliberately rather than re-litigated. **Metric:** `DailyMetricPoint.restingHeartRate` (BPM), already typed and already the exact accessor `buildRollingBaseline`'s own header comment names for this variant — no data-layer change needed. **Scale:** ONE shared, zero-based y-scale (not dual) — same reasoning as 4.3: the line and area are the same quantity, so a second axis would make "above/below baseline" meaningless. **Baseline:** computed client-side via `buildRollingBaseline`, `RHR_BASELINE_WINDOW_DAYS = 7` (matches 4.3's HRV window — HRV and RHR are scored from the same overnight cycle, so no reason to give one a shorter "recent normal"), `minSamples` = the function's own default of 3. **Colors:** RHR actual and HRV actual deliberately SHARE `--color-chart-7` (passes 4.5:1, no casing); RHR and HRV baseline SHARE `--color-chart-4` (fails 3:1, muted-hairline edge) — design.md §1's "HRV/RHR sharing" note confirms this is fine since the two never appear in the same chart. **Mapping:** supersedes design.md §4's locked RHR "population ideal-band" identically to 4.3's HRV supersession — DEFERRED pending Phase 5 cycle-day data; the band is labelled **"Recent baseline"**, never "Ideal RHR" (the old placeholder's label is corrected, not carried forward). **§5.2:** data-describing `<title>`/`<desc>` (date span, BPM range, "N of M days show a baseline", missing-RHR count), sr-only `ChartDataTable` (day / RHR BPM / recent baseline BPM), hover/focus tooltip parity + Escape on focusable RHR points, reduced-motion-gated fade — all identical machinery to 4.3, just relabelled. **Wiring (`src/App.tsx`):** `RhrBaselineTile` replaces the static `ChartContainer` + `.combo-chart-placeholder` in the `bento-rhr` tile (status mapping identical to `HrvBaselineTile`/`RecoveryStrainTile`: 401 or ready-with-no-RHR-in-window → empty, loading/error pass through), fed App's shared `useDailySeries(rangeDays)` — no new fetch. Dead `.combo-chart-placeholder` CSS removed from `App.css` now that both HRV and RHR tiles are real components (the 4.9–4.11 dead-CSS precedent); `.legend-swatch-actual`/`.legend-swatch-ideal` are unchanged and already correctly tokened for reuse. Gates (2026-07-28): `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run test:transforms`, `npm run format:check` all pass — no new test script, since `buildRollingBaseline` is untouched and its RHR-accessor case was already implied by its existing generic coverage. **Open residual — NOT live-verified against real `/api/daily-series` data** (the standing Phase 4 residual, same as every combo chart before it).

**Skills / knowledge to lean on**

- **Knowledge:** D3 scales (`scaleLinear`, `scaleBand`, `scaleTime`), `d3.stack` for stacked bars, `d3.area`/`d3.line` for combos, `d3.axis`, transitions; the React+D3 integration pattern (refs vs. React-managed SVG); responsive SVG with `viewBox` + ResizeObserver.
- **Tip:** Dot-matrix/calendar layouts are where D3 shines over Chart.js — worth the extra control you chose.

---

## Phase 5 — Daily questionnaire

**Goal:** Capture subjective daily inputs and store them alongside WHOOP data for correlation.

- [x] **5.1 Define questions** — e.g. mood, soreness, stress, alcohol, nutrition, motivation (Likert 1–5 or tags). Decide the schema. **Two HARD constraints from Phase 4.10 (2026-07-14, confirmed + strengthened 2026-07-18):**
  1. The "Period" field must be **tri-state** (`yes` / `no` / `not logged`), not a plain boolean — the period-meter's cycle-start-detection algorithm (now shipped in `src/lib/cycle.ts`; its `PeriodLog` type is the consuming contract) needs to distinguish "logged no period today" from "journal wasn't filled in that day," and a plain boolean can't represent that. See ROADMAP.md 4.10 for why.
  2. **Phase 5 must ask the user their typical cycle length ONCE, on their first logged period** (user decision 2026-07-18) — the app never assumes 28. Until that value exists (or ≥2 logged episodes let `estimateCycleLength` produce a real estimate), the 4.10 meter deliberately renders **text-only** ("Day 6", no denominator, no dot row). Wire the answer into `PeriodMeterTile`'s `typicalCycleLength` prop; once ≥2 episodes exist the estimate takes precedence and the stored answer becomes a fallback.
  - **Done (2026-07-29) — schema only; no form, no API route, no App.tsx wiring (those stay 5.2/5.3).** Two artifacts: `supabase/migrations/0004_daily_questionnaire_schema.sql` (new) and `api/_lib/journal-types.ts` (new). **The question set was NOT re-derived** — the eight questions and their order are read off `journal-stub-list` in `src/App.tsx` (the Figma-confirmed journal tile, design.md §3), treated as locked design: Hydrated, Cramps, Period, Discharge, Afternoon snack, Traveled, Caffeine, Alcohol. The bullet's own "e.g. mood, soreness, stress, motivation" suggestion is therefore **dead** — it matched 0001's placeholder columns, not the confirmed tile, and those columns are dropped rather than kept: a column nothing will ever write is worse than no column, since 5.5 would have to explain the empties.
  - **Type per question, and why** (the form is a once-a-day thumb-on-phone checklist, so one tap is the default answer shape; a question earns something richer than a boolean only when the extra taps buy signal 5.5's correlations would otherwise lose). **`hydrated boolean`** — the locked label is a self-assessed _state_, not a count of glasses; a litres field would be a different question than the one designed. **`cramps text`** in `none|mild|moderate|severe` — intensity _is_ the signal, and "had cramps" flattens a twinge and a day off work into one value; four levels rather than a 1–5 Likert because five self-reported gradations of pain aren't reliably distinguishable day to day and four chips fit one mobile row. **`period text`** — tri-state, see constraint 1 below. **`discharge text`** in `none|light|moderate|heavy` — the one genuine ambiguity, resolved explicitly below. **`afternoon_snack boolean`**, **`traveled boolean`** — binary events; it happened or it didn't (what _kind_ of travel, if it ever matters, goes in `extra`). **`caffeine_servings smallint`** and **`alcohol_drinks smallint`** — **dose, not presence**: "had caffeine" is true on nearly every day, which makes it useless as a correlate, while one espresso vs. four (and one drink vs. five — WHOOP recovery is famously alcohol-sensitive) is what actually moves HRV and sleep; `0` is an answered "none", so the boolean case is still one tap. `alcohol_drinks` replaces 0001's `alcohol boolean`. The three closed vocabularies are **text, not smallint codes**, so a raw `select *` is readable without a codebook and the TS unions mirror the stored values literally; ordinal display order lives in `CRAMP_LEVELS` / `DISCHARGE_LEVELS` (the single source of ordering, since a CHECK constrains vocabulary but not order). `notes text` and `extra jsonb` are **kept from 0001** as the no-migration escape hatch. **Every answer column is nullable and NULL means NOT ANSWERED** — never 0, never false, never `'none'`; `cramps: null` ≠ `cramps: 'none'`, and a CHECK is satisfied by NULL (null → unknown → no violation), so the vocabularies constrain values without ever forcing one.
  - **Ambiguity, stated rather than silently picked:** the stub list says only "Discharge" with no confirmed answer vocabulary — not covered by the tile or either hard constraint. **Resolution: amount (`none|light|moderate|heavy`), not a fertility-awareness texture vocabulary** (sticky / creamy / egg-white / …). Amount reuses the same four-chip control as `cramps` (one form idiom, not two), needs no user training, and texture tracking is a feature nobody has asked for; if it's wanted later it lands in `extra` first, no migration. Flag this at 5.2 if the Figma detail says otherwise.
  - **Constraint 1 (tri-state Period) — satisfied as `period text check (period in ('yes','no'))`, NULL = not logged.** Text over nullable boolean for two reasons: it mirrors `src/lib/cycle.ts`'s `PeriodLog` (`'yes' | 'no' | null`) **verbatim**, so 5.3's read path needs no mapping layer to get wrong; and a boolean invites exactly the forbidden bug — `coalesce(period, false)` reads as harmless tidying while converting "never opened the journal" into "no period today", and there's no comparable one-liner that turns NULL into `'no'`. Documented in the migration and in a column comment. **Enforced, not just documented:** `journal-types.ts` ends with two mutual `AssertAssignable` type aliases against `PeriodLog['period']`, so if either side gains or loses a member (say a `'spotting'` answer) `npm run typecheck:api` fails instead of the mismatch surfacing as a silently mis-detected cycle. Verified by temporarily widening `PeriodAnswer` to include `'maybe'` — typecheck failed with TS2344, then reverted and passed.
  - **Constraint 2 (cycle length asked once) — satisfied by a new `public.user_settings` table**, `user_id text primary key`, `typical_cycle_length smallint` (nullable; sanity-bounded 15–90 as a typo guard, explicitly not clinical guidance), `typical_cycle_length_asked_at timestamptz`, `created_at`/`updated_at`, RLS enabled with no policies like every other table here. It is **not** a per-day answer, so it is deliberately out of `daily_questionnaire`, where it would be re-asked or duplicated on every row. Not folded into `whoop_tokens` (the only existing one-row-per-user table) because that is a **credential store** — a settings write path touching the token row means a settings bug can damage auth. Named `user_settings` rather than `cycle_settings` so the next once-only scalar (a 5.4 reminder time, a units preference) is another nullable column, not another table. `_asked_at` exists so "asked once" survives a **dismissal**: without it, a user who declines gets re-prompted on every future period day. The column comment restates the precedence rule verbatim — **once ≥2 logged episodes exist, `estimateCycleLength()` wins and this stored value is a fallback only** (`cycleState()` already implements that precedence and reports `lengthSource`, so 5.2/5.3 pass the value straight into `PeriodMeterTile`'s `typicalCycleLength` prop and must not pre-resolve it themselves). Never defaulted to 28; NULL = never answered.
  - **Migration mechanics:** `0004_…` is a NEW file — 0001/0002/0003 are untouched, since they may already be applied (the 0003 precedent). It `alter`s rather than recreates: drops the five 0001 placeholders (`mood`, `soreness`, `stress`, `motivation`, `alcohol`), adds the eight real columns, and keeps `id` / `user_id` / `day` / `created_at` / `updated_at` / `unique (user_id, day)` exactly as they were, so "edit today" is still an upsert on `(user_id, day)`. **The drops cannot lose data** — the only references to `daily_questionnaire` in the entire repo are 0001 itself and documentation (verified 2026-07-29); no write path has ever existed. This is not a precedent for destructive migrations on the synced WHOOP tables, which hold real rows. Idempotent throughout (`drop column if exists` / `add column if not exists`; CHECKs are dropped-then-added by name, since PostgreSQL has no `add constraint if not exists`), `create extension if not exists pgcrypto` restated per 0001's portability habit, and every table/column carries a comment explaining intent. `period` is unquoted — it is **non-reserved** in PostgreSQL (checked against the current keyword appendix), unlike 0003's `"end"`. `vercel-env-setup.md`'s RLS verification list gains `user_settings` so the post-migration check still matches reality.
  - **Types (`api/_lib/journal-types.ts`, new):** placed beside `whoop-types.ts` / `transforms.ts` because the frontend already imports types from `api/_lib` (`useDailySeries`, the combo charts), so 5.2's form and 5.3's route share one declaration instead of each redeclaring an answer shape. Exports `JOURNAL_QUESTIONS` (the locked keys + labels **in tile order**, so 5.2 doesn't reinvent the ordering), `PeriodAnswer`, `CRAMP_LEVELS`/`CrampLevel`, `DISCHARGE_LEVELS`/`DischargeLevel`, the numeric bounds mirroring the CHECKs (`CAFFEINE_SERVINGS_MAX`, `ALCOHOL_DRINKS_MAX`, `TYPICAL_CYCLE_LENGTH_MIN`/`_MAX` — so the form rejects a bad value before the DB does; change both together), `JournalAnswers` (the writable subset = form state = upsert payload, no id/timestamps to invent), `DailyQuestionnaireRow`, and `UserSettingsRow`. The file's one import is **type-only** (`PeriodLog`, erased at build) and exists solely for the compile-time guards above; it holds no logic and no I/O.
  - Gates (2026-07-29): `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run format:check` all pass. **Migration applied to live Supabase 2026-07-30** (by you, via the SQL editor — the Supabase CLI is not installed on this machine, so `db push` was not the route; note that Supabase's "destructive operations" warning fires on any `drop`, here the five placeholder columns plus the drop-then-add CHECK idempotency pattern). Verified in-editor the same day against `pg_tables` + `information_schema.columns`: 7 tables with `rowsecurity = true` (`user_settings` among them), `daily_questionnaire` at 15 columns with none of the five placeholders remaining, `user_settings` at 5, and 0 journal rows. **This is the first Phase-5 migration applied ahead of its consumers** — 5.2/5.3 are writing against a schema that already exists live, not one that still needs applying. Nothing consumes these types yet by design: 5.2 (form) and 5.3 (API route) are the first consumers, and the journal tile in `src/App.tsx` is still the Phase 5 stub.
- [x] **5.2 Form UI** — accessible daily form, one entry per day, edit-today support, validation. **Done (2026-07-30) — UI only; persistence is session-local pending 5.3.** One new component, `src/components/JournalForm.tsx` (placed beside `form.tsx`/`states.tsx`, NOT barrel-exported from `components/charts` — that barrel is the Phase 4 charting foundation and this is a form, so it's imported directly the way `RangeToggle`/`ChartContainer` are). It is the **first consumer of `api/_lib/journal-types.ts`**: the question set, their ORDER, the two vocabularies and the numeric bounds are all read from `JOURNAL_QUESTIONS` / `CRAMP_LEVELS` / `DISCHARGE_LEVELS` / `CAFFEINE_SERVINGS_MIN|MAX` / `ALCOHOL_DRINKS_MIN|MAX` — nothing is retyped locally, and the questions are rendered by mapping `JOURNAL_QUESTIONS` itself through an **exhaustive switch on `JournalQuestionKey`**, so adding a ninth question there is a compile error here rather than a silently missing control.
  - **Self-contained by design (the 5.3 seam):** props are `day` / `initialAnswers?` / `onSubmit` / `submitting?` / `submitError?` — **the suggested prop shape shipped unchanged**, no deviation. The component performs no I/O and names no endpoint; 5.3 wraps it with a real load/save without editing it. "One entry per day" and "edit today" are therefore properties of the CALLER (the DB's `unique (user_id, day)`), expressed here as a single editable snapshot of one `day` pre-filled from `initialAnswers`, not an append-only log. Re-seeding is keyed on the **reference identity** of `initialAnswers` (plus `day`), adjusting state during render rather than in an effect — documented on the prop, because a caller that rebuilds the object inline every render would wipe in-progress typing. That is exactly how 5.3's async load reaches the form.
  - **Null discipline drove every control choice** (the point of the task, and the reason none of these are the obvious widget). **No checkboxes** for `hydrated` / `afternoon_snack` / `traveled`: a checkbox is two-state, so once touched it cannot say "not answered" and would invent a `false` the user never gave. They are tri-state radiogroups. `period` uses the identical idiom mapped to `PeriodAnswer | null` (`'yes'`/`'no'`/null, mirroring `src/lib/cycle.ts`'s `PeriodLog`). The counts hold **raw strings** in state and are parsed only on submit — `Number('')` is `0`, which is precisely the coercion that would turn "didn't answer" into "answered none". `notes` is trimmed, `''` → `null`. **`extra` has no UI and is round-tripped** from `initialAnswers`, so editing a day can't drop a field this form doesn't know about. `onSubmit` always receives all ten keys.
  - **DEVIATION FROM THE BRIEF, flagged:** the brief asked for the four-level chip groups (`cramps`, `discharge`) to represent the fifth state as **no chip selected**. They shipped instead with an explicit, selectable **"Not answered" chip** — the same idiom as the tri-states. Reason: a radio that is merely unchecked has **no route back to unchecked** once a chip is picked (radios don't deselect, and a Delete-to-clear key would be undiscoverable), so "no chip selected" would be a state a mouse user could never return to and a keyboard user could never reach — the exact data-corruption failure the requirement exists to prevent. Making `null` a first-class option keeps it reachable by every input modality, puts all five closed-vocabulary groups on ONE idiom, and keeps `aria-checked` honest. Verified in-browser: every group renders exactly one `aria-checked="true"`, defaulting to "Not answered".
  - **Validation:** only the two counts can be invalid. Out-of-range or non-integer input is **rejected with visible inline text before `onSubmit` is called — never silently clamped** ("Enter a number between 0 and 20, or leave it blank."), bounds taken from `journal-types.ts` so the form refuses what the 0004 CHECKs would refuse. **No field is required** — an entirely-null submit is valid, as the schema intends. The form is `noValidate` so native constraint bubbles can't compete with the linked `role="alert"` text (and can't claim a blank field is a problem); `min`/`max`/`step` remain set for the mobile keypad.
  - **Accessibility (§5.1 shell rules; §5.2's chart contract does not apply — this is a form):** each group is `<fieldset>` + `<legend>` around a `role="radiogroup"` named by that legend, containing native `<button role="radio">` with `aria-checked`; the **interaction model is copied from `RangeToggle.tsx`, not reinvented** — roving tabindex (one Tab stop per group), selection-follows-focus on arrows, Home/End. Every other control has a real `<label for>`; hints and errors are linked with `aria-describedby`, invalid fields carry `aria-invalid`, error text is `role="alert"` (the `ErrorState` precedent, which is reused verbatim for `submitError`), and a failed submit **moves focus to the first invalid field**. Chip borders are `--color-muted` (5.42:1) — §5.1 forbids `--color-border` as a control's only boundary — and selection is conveyed by `aria-checked` as well as the accent fill, so color is never the only signal. Chip transitions are gated on `prefers-reduced-motion`. Existing tokens only; no new colors.
  - **Supporting changes:** `src/components/form.tsx` gains a `Textarea` primitive (`.ui-textarea`, the `.ui-input` skin with `resize: vertical` so it can't escape its tile) and `Input`/`Textarea` move from bare `*HTMLAttributes` to `ComponentPropsWithRef` so the focus-first-invalid-field behavior can hold a ref (React 19 passes `ref` as an ordinary prop — no `forwardRef`; only the prop type needed widening). `Label`/`Select` keep their 3.3 signatures. Journal styles live in `components.css` beside the other primitives.
  - **Wiring (`src/App.tsx`):** new `JournalTile` replaces the static stub (`journal-stub-list`/`journal-stub-note` markup gone), holding today's answers in a `useState` and passing them straight back as `initialAnswers` — so **edit-today genuinely works within a session** and nothing is written anywhere. A four-point `TODO(5.3)` at that state names exactly what to replace (load → `initialAnswers` + `status="loading"`/`"empty"`; real awaited upsert → the `submitting`/`submitError` props the form already accepts but this stub leaves unset; drop the warning subtitle; the cycle-length prompt). Dead `.journal-stub-list`/`.journal-stub-note` CSS removed from `App.css` (the 4.9–4.11 precedent); `.journal-stub` survives **renamed** to `.journal-session-note`, because the subtitle no longer means "not built" — the form works, but entries don't outlive a reload and the tile says so ("Not stored yet — entries last until you reload (Phase 5.3)").
  - **Out of scope, deliberately, per the 5.1 note:** the **once-only "typical cycle length" question** (5.1 constraint 2) is NOT built. It writes `user_settings` and must first READ `typical_cycle_length_asked_at` to know whether it has already been asked — otherwise a user who declines is re-prompted on every future period day — and there is no API to ask yet. A TODO sits at the `period` control recording where it goes and that its answer feeds `PeriodMeterTile`'s `typicalCycleLength` prop **unresolved** (`cycleState()` owns the precedence). The 4.10 period meter therefore still rendered `no-data` as of 5.2: nothing persisted yet, so there was no log history to pass it. (**Closed in 5.7** — the meter reads its own history now; the once-only cycle-length question remains unbuilt, so `typicalCycleLength` is still passed unresolved.)
  - **OPEN CALL FOR YOU — desktop bento layout, not resolved unilaterally.** At ≥640px the journal tile is a narrow 3-row-spanning column (**measured: 219px wide, 1651px tall**), which stretches its row neighbours to ~550px of mostly whitespace and takes the grid to ~2261px. At mobile — the Figma design's native 430px frame — it is full-width (327px at a 375px viewport) and completely fine, so **this is a desktop-only consequence** of putting a 9-question form in a cell sized for an 8-item list. Three ways out, all design calls: (a) cap the form inside the tile and let it scroll (`ChartContainer`'s existing `bodyHeight` prop is the in-repo mechanism, but picking the number is a design decision and a fixed height would hurt mobile); (b) give the journal its own full-width row — edits the locked `grid-template-areas` (design.md §2); (c) move it out of `.bento-grid` entirely, the **4.14 precedent** for "this doesn't fit the locked grid". Shipped as-is in the tile because the task specified that placement and (b)/(c) change confirmed Figma layout. Say which you want.
  - Gates (2026-07-30): `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run format:check` all pass. **Verified in the dev preview** (plain `vite dev`; the chart tiles show their error states because there's no `/api` there — expected, and no mock was needed since this form makes no requests): all six radiogroups resolve `aria-labelledby` → `<legend>` inside a real `<fieldset>` with exactly one tabbable and one checked option; ArrowRight moves selection AND focus (verified "Not answered" → "Mild" on Cramps); a click mutates exactly one group (checked in isolation after an earlier stale-element-ref made it look otherwise); `99` caffeine blocks submit with `aria-invalid="true"`, a resolving `aria-describedby`, `role="alert"` text and focus on the bad field. **The null round-trip was verified end to end through save → parent state → re-seed:** caffeine `0` came back `"0"`, alcohol left blank came back `""` (NOT coerced to `0`), `hydrated: false` came back "No", `period: 'yes'` came back "Yes", and untouched fields stayed "Not answered" — i.e. `0` ≠ null, `false` ≠ null, and `'none'` ≠ null all survive a full edit-today cycle. No console errors; no horizontal overflow at 375px or 1280px; all chips ≥44px.
- [x] **5.3 Storage** — persist responses (same DB as Phase 2.4), keyed by date + user. **Done (2026-07-31) — mock-verified in the browser, not yet live-verified against production.** Two new files, `api/journal.ts` and `scripts/test-journal.mjs` (`npm run test:journal`), plus the `JournalTile` wiring in `src/App.tsx`. **No migration was written and none was needed** — 0004 has been live since 2026-07-30, so this is the first code to write a table that already existed. `JournalForm.tsx` was NOT touched: the 5.2 seam (`day` / `initialAnswers` / `onSubmit` / `submitting` / `submitError`) held exactly as designed, and 5.3 is entirely the caller's side of it.
  - **`GET /api/journal?day=YYYY-MM-DD` → `200 { entry: JournalAnswers | null }`; `POST /api/journal { day, answers }` → `200 { entry }`.** Modelled on `api/sleep-stages.ts` down to the helpers: same `parseCookies`/`json`, same auth check, same `DatabaseUnavailableError` / `isDbUnavailableStatus` → `503 { waking: true }` + `Retry-After` (Phase 2.5), same "log the detail with `console.error`, return a generic body" rule — no Supabase message, status code or table name reaches the client. 405 for anything but GET/POST.
  - **Identity is derived, never accepted.** `decodeSession()` on the `whoop_session` cookie is the ONLY source of `user_id`; the body's is ignored (the row is assembled field by field, so an unknown key can't reach the table). This matters more here than in the read-only endpoints: the service-role client bypasses RLS, so that filter is the whole boundary between two members' rows. The test asserts it adversarially — a POST carrying `user_id: 'member-999-attacker'` stores the SESSION's id.
  - **Upsert on `(user_id, day)`, not append** — `.upsert(…, { onConflict: 'user_id,day' })` against 0004's unique constraint, because Phase 5 is an "edit today" workflow. `updated_at` is stamped by the write path (0001's no-trigger convention); `created_at` is deliberately absent from the payload so re-saving a day doesn't rewrite when it was first logged.
  - **"No entry yet" is a 200 with `entry: null`, not a 404** — it is the normal state of this endpoint on any morning, and a 404 would make the tile render an error for the commonest case. The read uses `.limit(1)` + `[0]` rather than `.single()`/`.maybeSingle()`, so an empty result stays an ordinary empty array with no PostgREST error semantics to reinterpret.
  - **NULL DISCIPLINE, enforced on both sides of the wire and in both directions.** Validation is hand-written per field and returns an `INVALID` **symbol deliberately distinct from `null`**, so "the client sent garbage" (→ 400) can never collapse into "the user didn't answer" (→ a NULL column) — the one bug that would quietly fabricate answers. An absent/null field is written as an explicit NULL; nothing is defaulted; a wrongly-typed field is a 400 rather than a coercion (`hydrated: 'yes'` → 400, NOT `true`). Coming back, the response is built key by key, so a NULL column arrives as a present key with a `null` value instead of vanishing — `caffeine_servings: 0` and `caffeine_servings: null` stay different facts end to end, and `period: null` is never turned into `'no'`.
  - **The API does not trust the form.** Bounds and vocabularies are re-checked server-side against the same `journal-types.ts` exports the form validates with (`CAFFEINE_SERVINGS_MIN/MAX`, `ALCOHOL_DRINKS_MIN/MAX`, `CRAMP_LEVELS`, `DISCHARGE_LEVELS`, the `PeriodAnswer` union) — the endpoint is reachable without the form. `day` must be a real calendar date (the regex alone accepts `2026-13-45`), and the body has a 64 KB ceiling because `notes`/`extra` are free-form. Rejections are a generic `400 { error: 'Invalid request.' }` that doesn't enumerate the rules.
  - **`JournalTile` wiring:** mount-time GET behind a cancellation flag (the `useSleepStages` / `session-check` convention; same-origin fetch, so the cookie rides along without a `credentials` option, like every other call in `src/`), `ChartContainer status="loading"` in flight, `"empty"` on 401 (the 4.9 rule), `"error"` otherwise. `handleSave` awaits the upsert and drives `submitting`/`submitError`, rethrowing on failure so the form withholds its "Saved." status. The "Not stored yet — entries last until you reload" subtitle is gone, and `.journal-session-note` went with it from `App.css` (its own comment said to delete it when 5.3 landed).
  - **DEVIATION FROM THE BRIEF, flagged — the "keep the setAnswers call after a successful write" instruction had to be qualified.** Done literally it is a bug that no test can see: a new `initialAnswers` reference makes `JournalForm` re-seed, and re-seeding calls `setSaved(false)`, so the tile's own state update was ERASING the only confirmation a successful save produces. Caught in the browser (button back to "Save journal", status region empty). Fixed in the CALLER, since `JournalForm` was off-limits and correct: the tile adopts the server's echo only when it actually differs from what was submitted (`journalAnswersEqual`, field by field with `extra` serialized). When the server stored exactly what we sent — the normal case — the form already displays what is stored, so leaving the reference alone is both correct and what keeps "Saved." on screen.
  - **Tests (`scripts/test-journal.mjs`, the test-session.mjs pattern — real handler, mocked PostgREST via `global.fetch`, no creds, no network, no re-implementation):** 60 checks covering 401 on both verbs with the DB never touched, `entry: null`, a stored row with an explicitly-NULL column alongside an answered `0` and an answered `false`, out-of-bounds/out-of-vocabulary/non-integer/wrong-typed payloads (400 **and no upsert attempted**), the upsert request itself asserted against what supabase-js really emits (`on_conflict=user_id,day`, `Prefer: resolution=merge-duplicates`, the session's `user_id`, unanswered fields as explicit nulls), malformed `day` on both verbs, 540/503/0-status → waking vs. a genuine PostgREST 500 → flat 500, the Vercel pre-parsed-`req.body` shape, and 405.
  - Gates (2026-07-31): `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run format:check`, `npm run test:journal` all pass; `npm run test:transforms` and `npm run test:cycle` re-run clean. **Verified in the dev preview** through a temporary `vite.config.ts` middleware mock (the 4.1 trick — reverted afterwards, `git diff vite.config.ts` empty): a loaded row seeds the form with every null distinction intact, an edit+save POSTs the expected payload, a reload reads it back, the button reads "Saving…" and is disabled mid-flight then "Saved." after, a forced 500 shows the `role="alert"` message with the user's edits preserved and no false "Saved.", `entry: null` opens a blank form, and 401 renders the empty state with no form. No console errors. **Still NOT live-verified: nothing has yet written a row to the real `daily_questionnaire` from a logged-in browser** — the expected residual, same as every Phase 4/5 entry. The once-only cycle-length prompt remains unbuilt (see 5.1 constraint 2), and as of 5.3 the 4.10 period meter still rendered `no-data` because nothing read the stored `period` history back — **that read is 5.7's range endpoint**.
- [x] **5.4 Reminders** (optional) — daily prompt/notification to fill it in. **Done (2026-07-31) — IN-TAB reminders only, mock-verified in the browser across every branch; the real Notification permission prompt and a real OS notification are NOT verified (see the residual at the end).** Three new files — `src/lib/reminder.ts` (the rules, pure), `src/hooks/useJournalReminder.ts` (the browser shell), `src/components/JournalReminder.tsx` (the control) — plus `scripts/test-reminder.mjs` (`npm run test:reminder`), the reminder styles in `components.css`, and the `JournalTile` wiring in `src/App.tsx`. **`journal-types.ts`, `api/journal.ts` and `JournalForm.tsx` were NOT touched** — this is an additive layer above the 5.2 seam and beside the 5.3 caller, not a change to how the journal is asked or stored. No migration, no endpoint, no env var, no dependency.
  - **Scope, stated up front: this reminds you while the dashboard is OPEN in a tab, and never when it is closed.** A `Notification` raised from page script needs the page alive. Closed-browser push is a different feature (service worker + VAPID keys + a subscriptions table + a server-side scheduler) and is filed as **5.6** rather than half-built here — a half-built push path would look like a working reminder while silently never firing.
  - **No new endpoint and no second request.** The decision reuses the mount-time `GET /api/journal?day=…` that `JournalTile` already makes for the form (5.3); the tile passes the answer down as a `dayStatus`. `JournalTile` gained one piece of state for this, `logged: boolean | null`, deliberately NOT derived from `answers`: a save whose values match what was already on screen leaves `answers` untouched by design (5.3's `journalAnswersEqual`), and an entry of all-nulls is a real logged day that looks exactly like a blank one. `logged` is set from the load (`entry != null`) and set true after a successful upsert, so **saving today makes the reminder disappear immediately** — browser-verified.
  - **`src/lib/reminder.ts` is the whole policy, as ONE pure function** (`reminderDecision`) over four inputs — `today`, `dayStatus`, `permission`, the stored `preference` — returning `{ notify, prompt }`. Import-free and browser-free (the `cycle.ts` / `stats.ts` contract), which is the only reason any of this is testable: the Notification API cannot be exercised in Node, so the RULES were extracted from it and the shell around them kept thin. The hook and the component add no conditions of their own — that is stated in both files, because a stray `&&` in the component would move policy out of the one place a test can see it.
  - **The four rules, and the failure each one exists to prevent.** (1) **Never remind someone who already logged** — `dayStatus` must be an affirmative `'not-logged'`; `'unknown'` (still loading, a 401, a failed load) produces no notification AND no banner, because "we couldn't tell" is not "you haven't logged" (the same null discipline 5.1–5.3 run on, applied to a UI decision). (2) **Never auto-request permission on load** — the brief's dark-pattern rule, and self-interested too: the browser prompt is a one-shot resource and spending it unasked usually earns a permanent `denied`. `Notification.requestPermission()` is called in exactly one place, the opt-in button's click handler. (3) **Never nag when the browser said no** — `denied` shows a single explanatory line ONLY to someone who had opted in, and that line is dismissible for good; a `denied` browser that never asked for reminders sees nothing at all. (4) **At most one notification per calendar day**, gated on a stored `lastNotifiedDay` compared to today — not a timer and not a render count, so a reload, a re-render or a second tab can't produce a second nudge (the `tag` also collapses two tabs' notifications into one).
  - **DECISION, flagged rather than silently picked: the stored opt-in is a SECOND gate on top of the browser permission, not a mirror of it.** The brief says "if no entry exists AND the user has previously granted Notification permission" — read literally, `permission === 'granted'` alone would fire, and the persisted opt-in the brief also asks for would be decorative (the browser already persists permission). It ships as **both**: `granted` + opted in. Granting is not always our grant — an origin can hold the permission for an unrelated reason — and, more importantly, revoking a browser permission is buried in site settings, so without an app-level switch the only way to stop the nudges would be a trip through Chrome's settings. Consequence, stated: someone whose browser already granted notifications still has to press the opt-in once. If you'd rather have the literal reading, it's the `!preference.optedIn` branch in `reminderDecision` and one test case.
  - **`ReminderPrompt` has four states, one of which the brief didn't ask for.** `'opt-in'` (a real control, requesting permission only on click), `'blocked'` (the one-line denied note), `'none'`, and **`'on'` — an OFF switch, added deliberately**: an opt-in with no way back is its own dark pattern, and it doubles as the deep-link focus target below. Turning reminders off sets `optedIn: false, dismissed: true`, so the control stays gone; **known limitation — there is no settings screen, so re-enabling means clearing site data.** That is the honest cost of "dismissed means stop asking"; a future settings surface is where the re-enable toggle belongs.
  - **Persistence is localStorage**, one JSON object under `whoop-dashboard:journal-reminder` (the `whoop-dashboard:range-days` namespace from 4.14), holding `optedIn` / `dismissed` / `lastNotifiedDay`. Client-only on purpose: whether THIS browser may raise a notification is a per-browser UI choice, not user data — a phone and a laptop can legitimately disagree — so it has no business in `daily_questionnaire` or `user_settings`. Reads are field-by-field type-checked and **any** malformed value (absent, non-JSON, wrong shape, throwing storage in Safari private mode) falls back to the never-asked default, which is also the SAFE direction: nothing fires.
  - **Accessibility (design.md §5.1's shell rules; §5.2's chart contract does not apply — this is a control, not a chart):** the region is **always rendered** with `aria-live="polite"`, empty when there's nothing to say (`.journal-reminder:empty { display: none }` keeps the flex gap from opening up) — a live region has to exist BEFORE its content changes to be announced, the same reason `JournalForm`'s `.journal-status` is always in the DOM, and the states genuinely do change in place (opting in flips 'opt-in' → 'on', or → 'blocked' if the browser refuses). It carries `role="group"` + `aria-label="Journal reminders"` and `tabIndex={-1}` so it is a **real focus target** with a name — an unnamed generic announces nothing when a clicked notification focuses it. Every state is distinguished by its WORDS on an identical tinted panel, so nothing is encoded in hue alone; both actions are real `<button>`s via `Button`, inheriting the 44px `::after` hit area (measured: 44px against a 26.5px visual pill) and the shared `2px solid var(--color-accent)` ring — the legacy banner's undersized ✕ (§5.1's known exception) was deliberately not copied.
  - **Deep link:** clicking the notification calls `window.focus()`, closes it, scrolls the region into view and moves focus there — verified (scrollY 0 → 1815, `document.activeElement` = the region, notification closed).
  - **Tests (`scripts/test-reminder.mjs`, `npm run test:reminder` — the test-cycle.mjs pattern: real module, hand-built inputs, no browser):** 26 checks. Named cases for the fresh browser, an already-logged day under all four permissions, an unknown day status, the no-auto-request guard, the once-per-day gate (same day → no second nudge; yesterday → nudge), granted-but-not-opted-in, the denied/blocked/dismissed trio, and the unsupported API — plus an **exhaustive 144-case sweep** (3 dayStatus × 4 permission × 12 preferences) asserting three invariants: a notification requires all four conditions together, a logged-or-unknown day yields no UI either, and exactly 4 of the 144 combinations may fire (hand-counted). **What is NOT tested and why:** nothing here touches the Notification API, permission prompts, localStorage or the click→focus path — a mock of `new Notification()` would only prove the mock was called. Those are covered by the browser checks below, and the pure/shell split is what keeps that boundary honest.
  - Gates (2026-07-31): `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run format:check`, `npm run test:reminder` all pass; `npm run test:journal`, `test:cycle`, `test:transforms`, `test:stats` re-run clean. **Verified in the dev preview** through a temporary `vite.config.ts` mock (the 4.1/5.3 trick — an `/api/journal` middleware plus an index.html shim faking `Notification`, since the preview browser's real permission is fixed at `denied`; **reverted afterwards, `git diff vite.config.ts` empty**): the opt-in banner renders for `default` and fires nothing; pressing it stores `optedIn: true`, and when the browser grants, the region switches in place to the "on" copy and **exactly one** notification is constructed (title "Log today's WHOOP journal", tag `whoop-journal-2026-07-31`, body copy) with `lastNotifiedDay` recorded; a reload with that day already stored fires **zero**; a logged day fires zero and shows nothing, leaving `lastNotifiedDay` untouched; `denied` + opted in shows the blocked line, `denied` alone and `unsupported` show nothing; "Turn reminders off" empties the region (`display: none`, height 0, no leftover gap); **saving the form makes the control vanish while 5.3's "Saved." still appears**; no console errors; no horizontal overflow at 375px, where the two buttons wrap and keep their 44px targets.
  - **Residual — NOT live-verified, and only partly verifiable here.** The real permission prompt, a real OS-level notification, its click handler in the real notification centre, and the cross-day rollover have never run: the preview browser's permission is permanently `denied`, so every branch above was reached through a faked `Notification`. **Confirm on prod** by opting in, leaving the tab open on an unlogged day, and clicking the notification. Two known behaviours worth expecting rather than filing as bugs: a tab left open across midnight won't re-check until a reload (`today` is computed when the tile mounts), and Chrome on **Android** throws on the `Notification` constructor (it requires the service-worker path, i.e. 5.6) — that throw is caught, so the tile is unaffected and the day is still marked attempted rather than retried on every reload.
- [x] **5.5 Join with WHOOP data** — surface questionnaire fields in charts (e.g. dot-matrix correlating self-reported stress vs. recovery). **Done (2026-07-31) — mock-verified in the browser across every branch; NOT live-verified against real journal rows** (residual at the end). Three new files — `src/components/charts/HydrationRecoveryDotMatrix.tsx`, `src/lib/recovery.ts`, `src/lib/hydration.ts` — plus the join in `api/_lib/transforms.ts` / `api/daily-series.ts`, the `HydrationRecoveryTile` wiring and one constant move in `src/App.tsx`, new cases in `scripts/test-transforms.mjs`, the barrel export in `src/components/charts/index.ts`, the matrix styles in `charts.css`, one line in `components.css`, and the two design.md §4 notes this entry is cited from. No migration, no new endpoint, no new env var, no dependency. **`api/journal.ts`, `journal-types.ts` and `JournalForm.tsx` were NOT touched** — this is a read-side join above the 5.1–5.3 contracts, not a change to how the journal is asked or stored.
  - **THE BRIEF'S PAIRING DOESN'T EXIST — corrected, not silently substituted.** This bullet's own example, "self-reported stress vs. recovery", names a field that was never built: the locked 5.1 question set is hydrated / cramps / period / discharge / afternoon_snack / traveled / caffeine_servings / alcohol_drinks, and 5.1 explicitly killed the "mood, soreness, stress, motivation" suggestion as dead (it matched 0001's placeholder columns, not the Figma-confirmed tile).
  - **SHIPPED PAIRING: `hydrated` vs. `recovery_score` (your call, 2026-07-31).** It first shipped the same day as **`alcohol_drinks` vs. `recovery_score`** — 5.1's own argument, since alcohol is the one question deliberately typed as a **dose** rather than a state ("WHOOP recovery is famously alcohol-sensitive", and one drink vs. five is what moves the number) — and was swapped to hydration at your request. Everything below is the hydration version; the alcohol variant is one column swap away in `JournalMetricRow` / `JOURNAL_METRIC_COLUMNS` plus the encoding notes, should you want it back. **ONE pairing only**, per the task: a second would need its own honest summary sentence and its own argument, and two half-argued correlations are worse than one done properly.
  - **WHAT SWITCHING TO A BOOLEAN COST, stated rather than glossed.** `hydrated` is `boolean`, a self-assessed STATE — 5.1 chose that type deliberately, because the locked tile label asks how you felt, not how many litres you drank. Three consequences, all documented in the component header: (1) **no dose-response** — the chart compares two groups of days and nothing finer, where the alcohol version could show a ramp; (2) **a noisier instrument** — one self-assessment per day, with no scale to average out a misjudged one; (3) **the arrow genuinely points both ways** — feeling hydrated is as plausibly a CONSEQUENCE of a good recovery as a cause of one. The summary sentence was rewritten to claim exactly that much and no more ("not proof that either one causes the other", rather than the alcohol version's "not proof of cause").
  - **DATA LAYER — a join, not a second fetch (the 4.3/4.15 rule).** `buildDailySeries` takes a FIFTH input collection, `JournalMetricRow[]`, merged day-keyed exactly like the other four; `DailyMetricPoint` gains `hydrated: boolean | null`. `api/daily-series.ts` adds a fifth `readMetricRows` against `public.daily_questionnaire` over the SAME `(user_id, day)` window — the helper already generalized, so the read is four lines and the error classification (`DatabaseUnavailableError` → 503 waking) is inherited. `useDailySeries` / `DailySeriesState` needed **no change at all** beyond the widened type flowing through, and every consumer of the shared `useDailySeries(rangeDays)` call gets the field for free. The journal select is **column-narrowed to `day, hydrated`** on purpose: `daily_questionnaire` holds cramps, period, discharge and free-text notes, and none of that belongs in the payload for a recovery chart — widen it one column at a time, when a chart actually reads that column.
  - **The fifth parameter is REQUIRED, not optional.** `buildDailySeries(cycles, recovery, sleep, workouts, journal, range)` — a defaulted `journal = []` would let a caller drop the join and ship a series whose `hydrated` is null for reasons that have nothing to do with what the user logged, which is indistinguishable from "never answered" at every layer downstream. A caller with no journal rows passes `[]` and says so. Only two call sites exist (the endpoint and the test), both updated.
  - **NULL DISCIPLINE — the journal join is STRICTER than the WHOOP one, not looser, and a boolean raises the stakes.** There is deliberately **no `scored()` gate and nothing standing in for one**: a self-reported answer has no `score_state`, because it was either given or it wasn't. The merge is `jrnl?.hydrated ?? null` — `??` and never `||`, since `false ?? null` is `false` while `false || null` would silently destroy an answered "no". This is the exact coercion `hydrated boolean` invites and the same one 5.2 refused a checkbox over. Three places downstream had to resist it too: `summarize()` counts unanswered with `== null` rather than `!row.hydrated`; the tile's `hasData` check uses `p.hydrated != null`; and the sr-only table maps to the STRINGS `'Yes'`/`'No'`, because handing JSX a raw `false` renders an **empty cell** — an answered "no" would have become visually identical to a gap. `scripts/test-transforms.mjs` asserts the distinction adversarially (**Case 1b, 8 checks**, all strict `=== false` / `=== null` so a truthiness bug can't pass): answered `true`, answered `false`, a journal row that left the field blank → null, no journal row → null, a journal row on a day with **no WHOOP row anywhere** (the answer still lands — the join doesn't depend on WHOOP), an empty journal array → every day null with the WHOOP fields asserted byte-for-byte unchanged, and a journal row outside `[start, end]` appearing nowhere.
  - **CHART — `HydrationRecoveryDotMatrix`, barrel-exported with its props type** (the `HrvBaselineComboChart` / `RhrBaselineComboChart` / `DotMatrix` convention), new file, header-comment style copied from `DotMatrix.tsx`. One dot per calendar day across App's existing `rangeDays` window (30/90 — the 4.14 toggle drives it; no new window size was invented). It is a NEW component rather than a variant of `DotMatrix`: that one is a single-row progress meter with a scalar `filled` count, and nothing but the word "dot" is shared.
  - **TWO ORTHOGONAL CHANNELS, one per variable (revised 2026-07-31 at your direction — hue previously duplicated the recovery zone and now carries hydration instead).** **Hydration → the dot's COLOR**, three states with one legend entry each: _Hydrated_ (`--color-chart-1`), _Dehydrated_ (`--color-chart-2`), _Undetermined_ (`--color-border`). **Recovery → the dot's ROW**: a band per zone plus a dedicated bottom "no data" row, each labelled by its RANGE in real text on the left axis, which is §5.2 rule 4's "position … or direct labeling" satisfied without hue. Nothing is double-encoded across variables any more, and that is what makes the chart readable AS a correlation: hue answers "what did I log?", height answers "how did I recover?", and the question is whether the two line up. Rule 4 is still met for hydration because **radius repeats what the color says** (7px hydrated / 3.5px dehydrated / 5px undetermined) and undetermined additionally wears a dashed outline — so the answer survives greyscale, and the legend names the size channel in real text rather than leaving it to be inferred.
  - **The recovery zone hue is deliberately GONE from this chart.** `--color-positive`/`-warning`/`-negative` are not used here at all — they belong to the recovery donut's locked zone semantics, and having a dot be green-for-recovery while the legend says blue-means-hydrated would be two colour languages in one tile. The zone CUTOFFS are unchanged and still come from `src/lib/recovery.ts` (67/34, whoop-101-verified), so the rows a dot sorts into are exactly the donut's bands; only the paint moved. Two follow-on consequences, both handled: the tooltip and every dot's `aria-label` now name the band by its RANGE ("recovery 90%, 67–100% band") instead of WHOOP's hue word ("green zone", which the donut still uses correctly) — announcing a colour word for a band would contradict what the dot actually looks like, and the range is also exactly what the left axis prints, so what is heard matches what is seen.
  - **THE TWO NULLS STAY DISTINCT, and they now live on independent channels.** No recovery score → the dot **moves to the "no data" ROW**, keeping whatever hydration colour it earned (never skipped, never zero-filled, never dropped off the bottom of a zone). Hydration never answered → the **Undetermined colour plus a dashed outline**, deliberately NOT the same mark as an answered `false`, which is a small SOLID _Dehydrated_-coloured dot: "I logged that I wasn't hydrated" and "I never said" are different facts (5.1), and the one bug worth designing against here is a chart that quietly counts unanswered days as unhydrated ones. **The undetermined radius (5px) sits BETWEEN the two answers on purpose** — size must not imply which answer it would have been. Both null → an Undetermined dashed dot in the "no data" row.
  - **COLORS — design.md §1 checked first; NO new colors, and the three are named in ONE place.** _Hydrated_ = `--color-chart-1` (light blue, water); _Dehydrated_ = `--color-chart-2` (dark orange, dry/warm); _Undetermined_ = `--color-border`, deliberately NOT a data hue, because an unanswered day is an absence and a saturated colour of its own would make "I didn't say" look like a third kind of answer. §1 maps chart-1 to "Sleep" and chart-2 to "Calories"; neither appears in this chart, which is the same per-chart double-duty §1 already sanctions for chart-3 (skin temp vs. the period meter) — a note recording it was added to §1's soft-spots list. **Blue-vs-orange was picked over any other free pair on purpose:** it is the standard colorblind-safe opposition, and it matters more here than anywhere else on the dashboard because those two hues ARE the comparison. chart-1 (1.55:1) and `--color-border` (1.32:1) both fail 3:1 alone and are carried past it by the `--color-muted` hairline every dot and every `.legend-swatch` already wears (rule 4). The palette, the three legend labels and the `boolean | null` → state mapping live in the new pure **`src/lib/hydration.ts`**, which both the chart and App's legend import — so a dot and its legend entry cannot disagree about what a colour means. It is a separate module rather than an export from the `.tsx` for a concrete reason: `react-refresh/only-export-components` fails the build when a component file exports a constant, and `src/lib/recovery.ts` set the same precedent one bullet up.
  - **`RECOVERY_ZONES` + `recoveryZone()` still moved out of `src/App.tsx` into a new pure `src/lib/recovery.ts`** (the `cycle.ts` / `stats.ts` convention), values and lookup unchanged — this chart needs the CUTOFFS to place its rows, a chart importing from `App.tsx` would be a circular import through the charts barrel, and a re-derived "67" in a chart file is how one dashboard ends up with two definitions of a zone boundary. The left-axis row labels are DERIVED from that constant (a zone's upper bound is the next-higher zone's `min − 1`), so labels can never drift from the cutoffs the dots are sorted by, and they read `67–100%` / `34–66%` / `0–33%` / `no data`. design.md §4's pointer to the constant was updated with the move.
  - **The chart states the correlation in words, and refuses to overclaim.** One authored sentence is rendered BOTH as visible text and inside the SVG `<desc>` (same string, so they cannot drift): "Recovery averaged 71% on the 12 days you logged as hydrated and 58% on the 10 days you didn't — **an association in your own log, not proof that either one causes the other**." A day joins a group only when BOTH facts exist, so an unanswered day is never counted as "not hydrated". Below `MIN_DAYS_PER_GROUP = 3` in either group the sentence reports the day counts instead of naming an average — a "12% lower when not hydrated" computed off one bad Saturday sounds like evidence and isn't. That floor is deliberately looser than 4.12's `BASELINE_MIN_SAMPLES = 10`: 10 guards a headline shown alone, whereas this sentence sits beside all N dots and always states its own sample sizes. A second muted line reports coverage ("6 of 30 days have no hydration answer; 2 have no recovery score") so the reader can see how much of the window is actually saying anything.
  - **Sizing under pressure — ONE shrink factor, not three clamps.** At 90 days, or on a 375px phone, the columns get narrower than the dots; a single `fit` multiplier scales all three radii together so the hydrated/not-hydrated **ratio** — which IS the encoding — survives, where independently clamping each radius would flatten it exactly where columns are tightest (the first, alcohol-dose version of this chart did clamp independently, and measured a max radius of 4 against a floor of 2.5 at 375px). The column factor is 0.6, not 0.5, so the biggest dots may nudge into the neighbouring column: accepted deliberately, since the rows keep dots separated vertically and an encoding you can't see is worse than one that touches. `MIN_FIT = 0.57` stops the small dot dropping under 2px and ceasing to read as a mark.
  - **§5.2 in full:** `role="img"` + data-describing `<title>`/`<desc>` via `ChartSvg` (rule 1); sr-only `ChartDataTable` — day / hydrated / recovery — rendered from the SAME rows the dots are drawn from, where an answered `false` reaches the table as "No" and an unanswered day as "no data", so the distinction survives into the screen-reader output (rule 2); every dot is a Tab stop with the identical tooltip on hover and focus and Escape to dismiss (rule 3); hydration carried by radius and dash as well as hue, recovery carried by row position and its real-text axis label, plus the real-text summary sentence (rule 4); entrance fade gated on `prefers-reduced-motion` in JS (`chartTransitionDuration` → duration 0, `entered` starts true) AND in CSS (rule 5).
  - **PLACEMENT — a full-width row, NOT the "chart-6 / bento-strain slot" the task offered.** There is no free slot to fill: `bento-strain` holds the real 4.9 strain ring, and design.md §4's chart-6 is a row in the MAPPING table (the strain-matrix option locked 2026-07-09 "since Phase 5 does not exist yet"), not an empty grid area — §2's Figma-confirmed `grid-template-areas` has nine areas and all nine are occupied, so a tenth tile means editing a locked layout, which is a design call and not this task's. The full-width row below the bento is also the honest shape (design.md §2's "Layout gap" decision, the `SleepStagesTile` / `RecoveryStrainTile` precedent): 30–90 day columns need the width, and 5.2 already demonstrated what wide content does to the 640px grid. The strain matrix (4.6) is **not** superseded — it remains unbuilt, and this shipped as an addition.
  - **Wiring (`src/App.tsx`):** `HydrationRecoveryTile` fed App's existing shared `useDailySeries(rangeDays)` — the same call already feeding five tiles, no new fetch and no new loading machinery. Status mapping is `RecoveryStrainTile`'s EXACTLY: 401 → `empty` (the 4.9 rule), loading/error pass through, ready-with-no-recovery-and-no-hydration-anywhere → `empty`. **Recovery present but zero journal answers is deliberately NOT empty** — that is the expected first-run state, and hiding it behind a tile-level empty state would conceal the one screen that shows what filling in the journal buys; the chart renders every dot hollow and dashed at its true recovery row and its summary says "log hydration in the daily journal to build the comparison."
  - **Two layout defects found by measuring, one fixed here and one deliberately left.** (1) `.ui-chart-legend` had no `flex-wrap` — invisible with the existing two-entry legends, but this chart's six entries pushed content to 413px at a 375px viewport. **Fixed** (one line in `components.css`; no effect on the two-entry legends). (2) `.sr-only-table` is clipped but still **laid out**, so a long `nowrap` caption widens the page — a first draft of this chart's caption measured 984px and took `document.scrollWidth` to 1020 at a 690px viewport. Worked around here by keeping the caption to the same short length every other chart uses (what "no data" means per column rides in the `<desc>` instead). **The underlying defect is PRE-EXISTING and NOT fixed:** at 375px the HRV (463px), RHR (476px) and Recovery-vs-strain (410px) tables already overflow identically, so a horizontal scrollbar on mobile predates this task. It belongs to the shared 4.0 `.sr-only-table` rule and affects four charts — **flagged for you rather than changed as a side effect of 5.5.**
  - Gates (2026-07-31, re-run after both revisions): `npm run lint`, `npx tsc -b`, `npm run typecheck:api`, `npm run test:transforms`, `npm run format:check` all pass; `npm run test:journal`, `test:cycle`, `test:stats`, `test:reminder` re-run clean. **Verified in the dev preview** through a temporary `vite.config.ts` middleware mock (the 4.1/5.3/5.4 trick — **reverted afterwards, `git diff vite.config.ts` empty**): across 30 days the dots use EXACTLY three fills (12 chart-1 / 12 chart-2 / 6 border) and the three legend swatches compute to the same three values (`rgb(96,225,240)` / `rgb(135,92,0)` / `rgb(207,227,240)`); radii are exactly 7 / 3.5 / 5 at full width, with the dashed outline on undetermined days only; an answered `false` reads "No" in the sr-only table, NOT an empty cell, while only a genuine null reads "no data"; a null-recovery day sits in the "no data" row (cy 105) keeping its hydration colour; the y axis prints the four ranges, and the tooltip and aria-labels name the band by range ("Recovery: 90% (67–100%)" / "recovery 90%, 67–100% band") with no colour word; **real** keyboard Tab moved between dots and produced the same tooltip hover does, Escape dismissed it while keeping focus; a recovery-only window → every dot Undetermined-and-dashed plus the "log hydration in the daily journal" summary; at 375px the size ratio survives the shrink (4.5 / 2.25 / 3.21 at `fit` 0.643) and the legend wraps without overflow. 401 → the connect-WHOOP empty state and an all-null window → the run-a-sync empty state were confirmed on an earlier build of this tile, whose status mapping this shares line for line. No console errors; no horizontal overflow at 1280px or 690px, and at 375px only the pre-existing `.sr-only-table` case above.
  - **Residual — NOT live-verified, and not verifiable here.** Nothing has ever written a row to the real `daily_questionnaire` from a logged-in browser (5.3's own standing residual), so this chart has never met a real journal answer: every branch above was reached through mocked `/api/daily-series` payloads. **Confirm on prod** by logging a few days — including at least one explicit "No" and one day left unanswered — then checking that the "No" renders as a small _Dehydrated_-orange dot and the unanswered day as an _Undetermined_-pale dashed one. Two known limitations worth expecting rather than filing as bugs: on a 375px phone the radii compress to roughly 2.25–4.5px, so the size channel is weak there (the row position, tooltip and table are unaffected), and the comparison is a **descriptive two-group mean with no control for anything** — day of week, travel, illness and sleep debt all ride along uncontrolled, which is exactly why the sentence says "association … not proof that either one causes the other" and why no p-value or trend line is drawn.
- [ ] **5.6 Push notifications when the browser is closed** — the deliberate follow-up to 5.4, which reminds you only while a tab is open. Needs all of: a **service worker** (`ServiceWorkerRegistration.showNotification` — also the only path that works at all on Chrome for Android), **VAPID keys** as two more Vercel env vars, a **`push_subscriptions` table** keyed by user (endpoint + p256dh + auth, with removal on a 410 from the push service), a **server-side scheduler** to decide at a per-user hour whether that day is unlogged (the Hobby cron is once-a-day, so a real "remind me at 20:00 local" needs either a fixed UTC hour or a different plan), and a **notification-click handler in the worker** that focuses or opens the dashboard. Deliberately NOT started in 5.4: none of it is a small addition to the in-tab path, and a half-built version looks identical to a working one right up to the moment it never fires.
- [x] **5.7 Cycle-day meter wired to real journal data (the 4.10 ↔ 5.3 seam)** — **Done (2026-08-03) — mock-verified in the browser across every branch; NOT live-verified against real journal rows.** The period-meter tile had rendered its `no-data` placeholder since 4.10 ("no data yet — the Phase 5 journal isn't built") because nothing ever crossed the seam: `PeriodMeterTile` took `logs` as a prop that no caller passed, and `GET /api/journal` only served `?day=` — one row, while `cycleState` needs HISTORY (it recomputes episodes from the full log every time, by design). Four changes, two new files: **a range read on `api/journal.ts`**, **`src/hooks/usePeriodLogs.ts`** (new), **a clipped-episode guard in `src/lib/cycle.ts`**, and **the tile itself**. Plus `src/lib/day.ts` (new — `localTodayISO` moved out of `App.tsx` so the hook shares one definition instead of copying it) and new cases in `scripts/test-cycle.mjs` and `scripts/test-journal.mjs`. **No migration, no new table, no new index** — `unique (user_id, day)` from 0001 already provides the btree the range scan wants. `journal-types.ts`, the POST path and the 5.4 reminder layer are untouched.
  - **`GET /api/journal?from=&to=` → `200 { days: [{ day, period }] }`, ascending.** Named `from`/`to` and NOT `?days=N`: the single-day param is `?day=`, and `day` vs. `days` one letter apart is a live footgun, so the two shapes are **routed on which params are present** and mixing them (or sending half a range) is a 400 rather than a silent reinterpretation. Selects **only `day, period`**, never `ANSWER_SELECT` — this feeds a dot matrix, and shipping the user's notes/cramps/discharge to render it would be a privacy cost with no payoff. Span capped server-side at **400 days** (inverted ranges rejected too) so it can't be turned into an unbounded history dump. Everything else is the file's existing posture verbatim: `isValidDay` on both bounds, `user_id` from the session cookie and never the query, `DatabaseUnavailableError` → 503 `{ waking: true }` + `Retry-After: 5`, generic bodies with detail to `console.error`. **Tri-state preserved:** a NULL `period` serializes as `null`, never `'no'`, and days with no row are simply ABSENT — no placeholder days synthesized (deliberately unlike `/api/daily-series`, which needs a point per day for axis continuity).
  - **`usePeriodLogs` follows `useSleepStages` line for line** — same `loading` / `unauthenticated` / `error` / `ready` union, same `cancelled` flag on every `setState`, same never-throw-out-of-the-effect discipline. **Window: 100 days ending today** (~3 episodes, so `estimateCycleLength` has 2 gaps to average). It returns `PeriodLog[]` ready for `cycleState`, plus **the window's lower bound**, which is load-bearing rather than informational — see the guard below. The only shaping it does is renaming `day` → cycle.ts's `date` and coercing anything that isn't a literal `'yes'`/`'no'` to `null`; that fallback direction is the safe one, since `null` and `'no'` group identically and so a malformed value can never invent an episode.
  - **The clipped-episode guard (`dropClippedEpisode`, opt-in).** A hard 100-day bound can BISECT a period: the first `'yes'` day inside the window then looks like a cycle start and isn't, and with only 2 gaps to average one fabricated start moves the estimate by days (browser-verified: 32 unguarded vs. 29 guarded on the same fixture). The oldest episode is discarded when its start can't be **proven** genuine. **The boundary is derived from `detectEpisodes`' own rule, not chosen separately:** the nearest day the window could be hiding is `windowStart − 1`, which joins the episode iff `start − windowStart < EPISODE_GAP_DAYS` — so offset 0…2 is dropped and offset **exactly `EPISODE_GAP_DAYS` is KEPT**, because a hidden `'yes'` 4 days before a start already splits into its own episode. `EPISODE_GAP_DAYS` itself is **untouched**. Costs at most one episode; when the dropped one was the only episode the meter honestly falls back to `no-data`, since an unprovable start yields a `dayOfCycle` that is wrong rather than merely imprecise. `cycle.ts` stays **pure — zero imports, zero I/O** — and both existing call signatures still work unchanged (`windowStart` is an optional last parameter on `detectEpisodes` and `cycleState`).
  - **The tile.** Consumes the hook, passes `logs` (and the window bound) to `cycleState`, and now that it performs I/O drives `ChartContainer`'s `status` per the 4.9 rule — `'empty'` = 401/no session, `'error'` = the fetch failed, and a successful-but-dataless response is still the component's own `no-data` render, NOT `'empty'`. The stale comment claiming it stays at the default `'ready'` is gone. **Copy corrected:** the `desc` and `caption` both said the Phase 5 journal "isn't built yet", which is now false — they read "no period logged in the last 100 days". **`typicalCycleLength` is deliberately left unresolved with its TODO intact**: the `user_settings` endpoint (5.1 constraint 2) is still unbuilt, `cycleState` owns the precedence, and with ≥2 episodes the estimate wins anyway.
  - Gates (2026-08-03): `npm run build`, `npm run typecheck:api`, `npm run lint`, `npm run test:cycle`, `npm run test:journal`, `npm run format:check` all pass; `test:transforms` and `test:reminder` re-run clean. `test-cycle.mjs` gains cases 14–17 (clipped leading episode dropped and the skewed estimate restored; unclipped leading episode kept; the exact `EPISODE_GAP_DAYS` boundary kept at `==` and dropped at `<`; the only-episode-clipped → `no-data`; and omitting the argument reproducing pre-5.7 behaviour). `test-journal.mjs` gains cases 10–11, driven through the REAL handler with mocked PostgREST: the ascending `select=day,period` query bounded on both ends and filtered on the session `user_id`, NULL staying `null`, rows exactly `{ day, period }`, and 400s for a mixed `day`+`from`, half a range, a malformed or impossible bound, an inverted range and a 401-day span (400 days accepted) — with no Supabase call made for any of them. **Verified in the dev preview** through a temporary `vite.config.ts` middleware mock (the 4.1/5.3/5.4/5.5 trick — **reverted afterwards, `git diff vite.config.ts` empty**): the hook requests exactly `from=2026-04-26&to=2026-08-03` (a 100-day inclusive window), 3 episodes → "Day 5 of 29" with `lengthSource: 'estimated'`, 29 dots and the matching `<desc>`; 1 episode → the text-only "Day 7" with **no** dot row and no assumed 28; 0 → the corrected no-data copy on the decorative 28-dot track; 401 → "Connect your WHOOP account to see your cycle day."; 500 → the error state; and the clipped fixture rendering 29 rather than the unguarded 32. No console errors.
  - **Residual — NOT live-verified.** Nothing has ever written a row to the real `daily_questionnaire` from a logged-in browser (5.3's standing residual), so the meter has still never met a real logged period. **Confirm on prod** by logging "Period: yes" on a couple of days: one logged day → "Day _n_" text-only, and only after a second episode ≥4 days later does the dot matrix with an estimated length appear. The once-only cycle-length prompt (5.1 constraint 2) stays unbuilt, so `lengthSource: 'user-reported'` remains unreachable in the UI.

**Skills / knowledge to lean on**

- **Knowledge:** form state + validation in React (controlled inputs or a form library), date-keyed records, joining subjective + objective time series.
- **Tooling — scheduled tasks:** I can create a recurring daily reminder.

---

## Phase 6 — Quality, security & deployment (added)

**Goal:** Make it robust and shippable. This is the part most personal projects skip and later regret.

- [ ] **6.1 Secrets management** — WHOOP Client Secret, Supabase keys, and the token-encryption key all live in **Vercel env vars**, never in the bundle. Set separate values for Preview vs. Production. Rotate the WHOOP secret if it ever leaks.
- [ ] **6.2 UI/UX redesign — responsive breakpoints & sleep-stage 3-month view (blocked on assets from Amaya)** — split out of the 2026-08-01 UI/UX punch list because these two can't be built to spec yet, unlike the rest of that list (journal tearsheet, chart color/token swaps, recovery/strain average indicators, glass-effect tiles, connection tile resize, Roboto, status-pill removal — all shipped ahead of this task via a separate Claude Code prompt, not gated on it): (a) **dedicated desktop/laptop/tablet layouts** — today `.bento-grid` is capped at 640px and centered at every breakpoint (design.md §2), so there is no real desktop/laptop treatment, only mobile (<640px) and one shared "tablet+" (≥640px) layout; needs Figma frames per target width before implementation starts. (b) **Sleep-stages chart, 3-month view** — bars narrow so each night reads closer to a thin line than a bar; needs a visual reference/sketch from Amaya to disambiguate from a plain line-chart conversion before implementation starts.
- [ ] **6.3 Error handling & logging** — graceful API failures, token-expiry recovery, Supabase-pause "waking up" state, user-visible error states. **Scrub logs** — never log tokens or health fields into Vercel/Supabase logs.
- [ ] **6.4 Testing** — unit tests for data transforms, component tests for charts, an integration test of the OAuth flow (mock WHOOP).
- [ ] **6.5 CI/CD** — Vercel auto-deploys on push (preview per branch, prod on `main`). Add GitHub Actions for lint + test on PR if you want a gate before merge.
- [ ] **6.6 Deployment** — production on Vercel (frontend + `/api` functions together); add the Vercel prod URL as a redirect URI in the WHOOP dashboard. Confirm Supabase prod project + region.
- [ ] **6.7 Privacy** — health data: keep the repo private, app-level token encryption already in Phase 1.4, store the minimum you need, region nearest you. _(Optional, max privacy: Supabase is self-hostable, but the managed-cloud security controls aren't available out-of-the-box when self-hosting — you'd harden it yourself.)_

**Skills / knowledge to lean on**

- **Knowledge:** GitHub Actions, environment promotion (dev→prod), basic web security (secret handling, CORS, HTTPS), health-data privacy hygiene.

---

## Phase 7 — WHOOP 5.0 local deep-metrics investigation (R&D track)

**Goal:** Determine whether — and how — we can source the _deep_ WHOOP 5.0 metrics (**recovery, strain, sleep**) **locally off the band over Bluetooth**, the way 4.0-only projects already do, so the dashboard could eventually run without the WHOOP cloud. This is a **parallel research track**, not a dependency of Phases 0–6.

> **Status & honesty (June 2026):** This is **exploratory** and may not pan out on any fixed timeline. As of now, **no public project has fully reverse-engineered the 5.0's deep-metric protocol.** Verify current project status before relying on any claim here — the space moves fast and my knowledge of it is not authoritative.

**What's possible on a 5.0 _today_ vs. blocked:**

- ✅ **Live heart rate** streams over the _standard_ Bluetooth Heart Rate Service (commonly UUID `0x180D` — _verify_), which needs **no pairing/bond**. This is the one piece available right now and the natural first proof-of-concept.
- ⛔ **Recovery, strain, sleep, history offload** ride WHOOP's _proprietary, encrypted_ protocol, which on the 5.0 is **not yet decoded** publicly.
- ⚠️ **Bond constraint:** the strap holds an encrypted Bluetooth bond with **one device at a time** (normally the official WHOOP app). Reading the deep data requires taking that bond, which can disrupt the official app's pairing.

**Investigation steps:**

- [ ] **7.1 Track the field** — follow the projects working on this and watch for 5.0 progress: `noop-app/noop` (4.0 + _experimental_ 5.0/MG), `bWanShiTong/openwhoop` (4.0, Rust), `bWanShiTong/reverse-engineering-whoop` + its writeup, `johnmiddleton12/wearable`, `madhursatija/whoof`, `christianmeurer/whoop-reader`, `jogolden/whoomp`. _(All 4.0-centric today; the 5.0 is the open frontier.)_
- [ ] **7.2 Capture your own BLE traffic** — the core RE method: enable **Bluetooth HCI snoop logging** on Android (Developer Options) — or use macOS **PacketLogger** — while the official WHOOP app syncs your 5.0, then analyze the frames in Wireshark. This is how the 4.0 was mapped.
- [ ] **7.3 Live-HR proof-of-concept (do this first)** — build a tiny local collector that reads the standard HR stream off the 5.0 and writes it to Supabase. Proves the _whole_ band→DB→dashboard path end-to-end **before** any deep-metric decoding exists, and is guaranteed to work today. (I can scaffold this in Node or Python whenever you want the quick win.)
- [ ] **7.4 Pairing / bond handshake** — investigate the encrypted bond (NOOP describes a `CLIENT_HELLO` link-establishment handshake on the 5.0). Understand single-device bonding and how to pair without permanently breaking the official app.
- [ ] **7.5 Decode the deep-metric packets** — once captured, map the 5.0 frame format and packet types for recovery/strain/sleep. Use the documented 4.0 layout (openwhoop's "type-47" biometric decode) as a _starting hypothesis only_ — expect the 5.0 to differ.
- [ ] **7.6 Local analytics** — recompute recovery/strain/sleep on your own machine from raw signals using documented methods (HRV RMSSD / Task Force 1996, Edwards or Banister TRIMP for strain, on-device sleep staging), mirroring what `openwhoop` and `noop` do. These are **approximations**, not WHOOP's proprietary scores, and are **not medical-grade**.
- [ ] **7.7 Wire it into the dashboard** — when/if decoded, stand up the **local collector → Supabase** path and reuse a **mapping/view layer**: the collector writes its own native tables, SQL views re-present them under the names the charts already query (Phase 2.4 shapes), so the dashboard swaps source with **no chart changes**.
- [ ] **7.8 Guardrails** — own device, own data only; interoperability/research framing (e.g. 17 U.S.C. §1201(f) reverse-engineering for interoperability); **not a medical device**; don't violate any agreement that binds you. Keep this track in a clearly-labeled, isolated module so it never touches the cloud pipeline's secrets.

**Skills / knowledge to lean on**

- **Knowledge:** BLE/GATT (services, characteristics, notifications), HCI snoop-log capture + analysis (Wireshark), packet framing/CRC, encrypted bonding/pairing, time-series signal processing, the published HRV/strain/sleep algorithms above.
- **Tooling:** I can scaffold the live-HR collector (7.3) and help analyze captured BLE logs.

---

## Suggested build order (critical path)

1. **Phase 0** (repo + scaffold) →
2. **Phase 1** (auth) — nothing works without a token →
3. **Phase 2** (data layer) — get real data flowing →
4. **Phase 3 + 4** in parallel (style shell while building the charting foundation) →
5. **Phase 5** (questionnaire) →
6. **Phase 6** (harden + deploy).

**Phase 7** (5.0 local deep-metrics R&D) runs **off to the side** — _not_ on the critical path. Pick it up opportunistically, starting with the live-HR proof-of-concept (7.3).

Get **one** chart end-to-end (auth → fetch → transform → render) before building the other five — it de-risks the whole pipeline.

---

## What I can do for you next

- Scaffold the repo + Vite app + `/api` functions and wire up Vercel + Supabase, then commit the baseline.
- Build the OAuth functions with encrypted token storage (Phase 1) and a typed WHOOP API client (Phase 2).
- Generate the design system with `theme-factory`, or mock the dashboard in Figma first.
- Build the reusable D3 charting foundation, then each chart.
- Stand up the questionnaire and a daily reminder.

Tell me which phase to start on, and confirm the chart→metric mappings in Phase 4.

---

## Sources

- [Overview — WHOOP for Developers](https://developer.whoop.com/docs/developing/overview/)
- [OAuth 2.0 — WHOOP for Developers](https://developer.whoop.com/docs/developing/oauth/)
- [Getting Started — WHOOP for Developers](https://developer.whoop.com/docs/developing/getting-started/)
- [WHOOP API Docs](https://developer.whoop.com/api/)
- [v1 to v2 Migration Guide](https://developer.whoop.com/docs/developing/v1-v2-migration/)
- [Recovery — user data](https://developer.whoop.com/docs/developing/user-data/recovery/)
- [Webhooks — WHOOP for Developers](https://developer.whoop.com/docs/developing/webhooks/)
- [Authenticating with WHOOP (Passport)](https://developer.whoop.com/docs/tutorials/access-token-passport/)
- [Lee et al., "A novel method for quantifying fluctuations in wearable-derived daily cardiovascular parameters across the menstrual cycle" — npj Digital Health, 2024](https://www.nature.com/articles/s41746-024-01394-0) (primary source for the HRV/RHR "ideal" band, Phase 2.6)
- [WHOOP: "New WHOOP Research on the Menstrual Cycle"](https://www.whoop.com/us/en/thelocker/new-whoop-research-on-the-menstrual-cycle/) (secondary summary of the above)

**Phase 7 (5.0 local R&D) references — unofficial community reverse-engineering projects:**

- [NOOP — local-first WHOOP companion (4.0 & experimental 5.0)](https://github.com/noop-app/noop)
- [openwhoop — WHOOP 4.0 local client (Rust)](https://github.com/bWanShiTong/openwhoop)
- [Reverse Engineering Whoop 4.0 — writeup](https://github.com/bWanShiTong/reverse-engineering-whoop-post)
- [bWanShiTong/reverse-engineering-whoop](https://github.com/bWanShiTong/reverse-engineering-whoop)
- [johnmiddleton12/wearable](https://github.com/johnmiddleton12/wearable)
