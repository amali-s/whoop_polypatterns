# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets — NEVER commit these.
# Only .env.example (placeholders) is tracked.
.env
.env.*
!.env.example

# Vercel
.vercel

# Captured WHOOP API samples (real personal health data — NEVER commit).
# Used only locally to derive types in api/_lib/whoop-types.ts; the derived
# field names + TS types are committed, the raw values here are not.
whoop-samples/

# Supabase local development
.supabase
supabase/.branches
supabase/.temp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env*
